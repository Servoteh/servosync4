# Dubinski review — PROIZVODNO JEZGRO (MES)

Baza koda: `C:\Users\nenad.jarakovic\wt\robno-quality\backend\src`
Obuhvat: `work-orders`, `tech-processes`, `pdm`, `mrp`, `handovers`, `plan-proizvodnje`, `plan-montaze`, `kvalitet`, `part-locations`
Datum: 2026-08-04 · read-only (nijedan fajl nije menjan)

**94 nalaza · 26 🔴 / 50 🟠 / 19 🟡**

| kategorija | jezgro (WO/TP/handovers) | prateći moduli | ukupno |
|---|---|---|---|
| 1. Tiho pogrešan podatak | 15 | 20 | **35** |
| 2. Konkurencija | 7 | 8 | **15** |
| 3. Poslovna rupa | 6 | 8 | **14** |
| 4. 500 umesto jasne poruke | 5 | 9 | **14** |
| 5. Performansa pri rastu | 7 | 9 | **16** |

Poglavlja 1–5 su jezgro (`work-orders`, `tech-processes`, `handovers`) — čitano linijski.
Poglavlje 6 nosi isti raspored kategorija za `kvalitet`, `part-locations`, `montaza-neusaglasenosti`, `pdm`, `mrp`, `plan-proizvodnje`, `plan-montaze`.
TOP 10 na kraju je jedinstven za ceo domen.

---

## Presuda o već poznatim stavkama (traženo: potvrdi/ospori)

| stavka | presuda |
|---|---|
| `work-orders.service.ts:1657` `bulkClone` — petlja bez `take`, pod `pg_advisory_xact_lock(targetProjectId)` | **POTVRĐENO, gore nego opisano.** `$transaction` na :1604 nema `timeout` → Prisma default **5 s** (nema ni globalnog override-a — `prisma.service.ts` je goli `PrismaClient`). Po RN-u ide ~10 round-trip-ova (1 create + 4 findMany + do 4 createMany + priorityByCode). Predmet 7701 ima >2.200 naloga ⇒ istek je siguran, ne moguć. |
| `pdm-import.service.ts:386/412` — ugnježdene petlje, tx već ima `timeout: 120_000` | Potvrđeno da `timeout: 120_000, maxWait: 10_000` postoji (`pdm-import.service.ts:199`). Detalji u §PDM. |
| `tech-processes.service.ts` 4906 linija / 9 klastera / 2 cross-domain reach-in-a | **POTVRĐENO i dopunjeno:** reach-in-i su `WorkOrdersService` (:391, zove `createQualityChildOrder`) i `QualityService` (:388). Teža posledica veličine nego sam broj linija: **ceo fajl nema NIJEDAN `pg_advisory_xact_lock`** — sav kiosk write-path (scan/control/start/stop/storno) je neserijalizovan, dok ga hladnije putanje (`work-orders`, `handovers`, `kvalitet`, `part-locations`) imaju. |
| Numeracija RN-a — jedan izvor? | **OSPORENO da problem još postoji — danas JESTE jedan izvor.** `WorkOrderNumberingService.next` je jedina numeracija; `handovers.service.ts:1516` je poziva (kopija je uklonjena, komentar :1511-1515 pamti incident 030/26). DB mreža `uq_work_orders_project_ident_variant` postoji. **ALI:** `bulkClone` (`work-orders.service.ts:1659-1662`) i dalje sklapa `identNumber` RUČNO (`${projectNumber}/${tail}`) mimo servisa, i to je 4. mesto koje zna format ident-a. Nije duplikat brojača (cilj mora biti prazan predmet), ali JESTE kopija formata koja preživi budući fix u servisu. |

---

## 1. Tiho pogrešan podatak u proizvodnji

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `tech-processes/tech-processes.service.ts:1952` + `:2105` | 🔴 | Plan RN-a je 10 kom. Dva radnika na dve mašine istog RC skeniraju prijavu rada po 8 kom u istoj sekundi. `assertPieceCountWithinPlan` radi `aggregate` PRE inkrementa; u READ COMMITTED nijedna transakcija ne vidi tuđi nekomitovan `{increment}` → obe pročitaju `existingSum=0`, obe prođu `8 ≤ 10`, obe inkrementiraju ⇒ **16 od plana 10**. Tvrdi guard „kucanje preko plana nije dozvoljeno" (Nenad 16.07) probijen bez ijedne poruke. | Oba skena vrate 200. `reachedPlan` se računa iz post-inkrement vrednosti pa drugi sken samo uredno zatvori operaciju. Prekoračenje postoji samo kao broj na kartici TP-a koji niko ne sabira. |
| `tech-processes/tech-processes.service.ts:4418` + `:4489`, uz `prisma/schema.prisma:1983` | 🔴 | Dva radnika PRVI put skeniraju istu operaciju svežeg RN-a istovremeno. `findRoutingTp` obojici vrati `null`, oba `create` prođu — `TechProcess` ima samo `@@index` na petorci `(projectId, identNumber, variant, operationNumber, workCenterCode)`, **NEMA `@@unique`**. Nastanu DVA reda za istu operaciju. Radnik A zatvori svoj na 10/10; red B ostane otvoren sa 0 → `markWorkOrderIfComplete` (`:4818`) traži da SVI značajni redovi budu `isProcessFinished` ⇒ **RN nikad ne pređe u „Završen"**, a „Moji otvoreni" trajno prikazuje fantomsku operaciju. | Oba skena su uspešna, komadi su tačni (kumulativ se sabira preko svih redova). Simptom stigne danima kasnije kao „nalog neće da se zatvori" i deluje kao problem kontrole, ne skena. |
| `tech-processes/tech-processes.service.ts:3524-3549` + `:3554-3572` + `:3734` | 🔴 | Kontrolor pritisne „Sačuvaj" dvaput na kiosku (touch, spor odgovor) ili mu istekne mreža pa ponovi. Nema idempotencije ni lock-a: `part_locations` dobije DVA `+10` reda (**polica pokazuje 20 od 10 komada**), `tech_processes` dobije 20, a za škart se POSLE komita dvaput zove `createQualityChildOrder` ⇒ nastanu **`-S1` I `-S2` za isti fizički škart**. | Oba odgovora izgledaju uspešno i nose istu nalepnicu. Višak na polici primeti tek popis; duplirani škart-RN tek planer koji se pita zašto dva naloga za istu stvar. |
| `tech-processes/tech-processes.service.ts:3982-4051` (`storno`) | 🔴 | Kontrolor je pogrešno otkucao 10 kom završne kontrole; šef radi storno 10. Nastane kontra-red `−10`, ali: **(a)** `part_locations` `+10` OSTAJE (grep potvrda: `partLocation.create` postoji SAMO na `:3561`, nigde reverse) — polica i dalje tvrdi 10 kom; **(b)** `markWorkOrderIfComplete` se NE poziva pa `work_orders.status` ostaje `true` — nalog i dalje „Završen"; **(c)** `work_order_operations.priority` ostaje 255 pa se operacija ne vraća na plansku tablu; **(d)** child `-S`/`-D` RN nastao tom kontrolom ostaje živ. | Storno vrati 200 sa `storniranoKomada: 10` i kartica TP pokaže neto 0 — jedna od četiri evidencije se ispravi, tri i dalje tvrde da je posao gotov. |
| `session-auto-close.service.ts:263-273` | 🔴 | Radnik druge smene START-uje u 22:10 (pon), zaboravi STOP, izlazi na kapiju u 06:05 (uto). Upit traži izlaz uz `event_ts_local::date = (started_at AT TIME ZONE tz)::date` = **PONEDELJAK** → utorkov izlaz se ne vidi. Sesija se zatvara sa `stoppedAt = startedAt` (**0 minuta**) i notom „NEISPRAVNO KUCANJE", a **šefu ide mejl da radnik nije kucao izlaz**. Cela noćna smena nestane iz evidencije, uz disciplinski ton. | Radnik sesiju ionako nije video. Mejl izgleda kao ispravan nalaz („nema izlaza"), ne kao bag u upitu — pa se ispravlja radnik, ne kod. |
| `tech-processes/tech-processes.service.ts:4831-4835` | 🟠 | RN ima DVE `significant_for_finishing` operacije (npr. „Završna kontrola" + „Kontrola pakovanja"), plan 100. Na prvoj otkucano 60, na drugoj 60. `controlledTotal` sabira redove **preko različitih operacija** ⇒ 120 ≥ 100 ⇒ `work_orders.status = true` iako nijedna kontrola nije obradila 100. Ispravna mera je MIN po operaciji, ne SUM preko svih. | Broj 120 se nigde ne prikazuje; UI vidi samo zeleni bedž „Završen". Radi ispravno na tipičnom nalogu sa tačno jednom završnom kontrolom, pa test ne hvata. |
| `tech-processes/tech-processes.service.ts:4831` | 🟠 | Plan 100; kontrolor otkuca svih 100 kao **ŠKART** (`qualityTypeId=2`). `controlledTotal` ne filtrira kvalitet ⇒ 100 ≥ 100 ⇒ nalog „Završen" sa **nula dobrih komada**. | „Gotovost RN" (`rnProgress:1610`) isti nalog meri kao 0% jer TAMO filtrira `quality_type_id = GOOD`. Dva ekrana tvrde suprotno; niko ih ne poredi. |
| `work-orders/work-orders.service.ts:499-517` (`resolveLocations`) | 🟠 | Kontrolor knjiži 5 kom **škarta** na policu A-12. `groupBy` nema `qualityTypeId` ni u `by` ni u `where` ⇒ kartica RN-a pod „Lokacije" prikazuje `A-12: 5` bez oznake kvaliteta. Magacioner ode po 5 dobrih, nađe 5 škarta. | Podatak o kvalitetu ne stiže do UI-a uopšte — nema šta da pobudi sumnju. Filtrira se samo `> 0` neto. |
| `session-auto-close.service.ts:246-249` | 🟠 | Cron u 07:00 pokupi 120 visećih sesija jednim `findMany`. Petlja traje minutima (sy15 upiti + mejlovi). U 07:04 radnik sa kioska uradi „Kraj rada" na svojoj jučerašnjoj sesiji i evidentira 6 h + 8 kom. U 07:06 petlja dođe do te sesije i radi `update` **BEZ `where: { stoppedAt: null }`** ⇒ prepiše `stoppedAt` na `startedAt` (0 h) i notu na „NEISPRAVNO KUCANJE". | Komadi ostaju na `tech_processes` pa naizgled sve štima; nestane samo vreme. Radnik je već otišao sa kioska. |
| `tech-processes/tech-processes.service.ts:3047-3050` (`dismissEntry`) | 🟠 | Radnik u 07:00 slučajno skenira START na tuđoj operaciji, primeti u 15:00 i pritisne „Odustani". Kod zatvara sesiju sa `stoppedAt = now`, **bez `autoClosed`, bez note** ⇒ `v_work_sessions` (tabovi „Sesije dnevno/zbirno/po satu") dobije **8 h rada** na operaciji na kojoj nije urađeno ništa. | Docstring (`:2985`) i UI izričito tvrde „NE akumulira komade — ni vreme"; odstupanje živi samo u analitici sesija koju radnik ne gleda. |
| `tech-processes/tech-processes.service.ts:1122-1135` (`card`) i `:1480` (`workerPerformance`) | 🟠 | Struganje traje 3 radna dana = 3 čiste START/STOP sesije po 7 h u `work_time_entries`. Ali oba prikaza vreme izvode iz `finished_at − entered_at` na `tech_processes` ⇒ ~68 h > `TP_PRIKAZ_MAX_SEC` (24 h) ⇒ **red se IZBACUJE u celosti**. „UKUPNO VREME" na kartici pokaže samo kratke operacije; 21 h stvarno IZMERENOG rada nestane. Merodavan ledger (`work_time_entries`) se u oba prikaza uopšte ne čita. | `excludedRowCount` se prikazuje kao higijena („zaboravljene prijave"), pa deluje kao ispravno filtriranje, a ne kao gubitak realnog rada koji leži u susednoj tabeli. |
| `tech-processes/tech-processes.service.ts:2233-2244` (`finish`) | 🟠 | Red operacije ima 5 kom. Radnik skenira +3 (red = 8). U istom trenutku poslovođa na „Zatvori operaciju" pošalje `pieceCount: 5` (broj koji je video pre skena) — `finish` je **jedini** write-path koji piše `pieceCount` APSOLUTNO, ne `{increment}` ⇒ tri komada nestanu. | Oba poziva vrate 200 i oba pokažu „ispravan" broj sa svoje strane. |
| `tech-processes/tech-processes.service.ts:1601-1611` vs `:4797` | 🟠 | Legacy RN čiji `tech_processes` redovi imaju `work_order_id = 0` (komentar na `:4758` kaže da je to „često"). `markWorkOrderIfComplete` spaja po TROJCI → digne `work_orders.status = true` → lista RN-ova kaže **„Završen"**. `rnProgress.made_good_final` spaja po `t.work_order_id = op.work_order_id` → **0** → tab „Gotovost RN" pokaže 0% i `isCompleted: false`. Dva „kanona" spajaju po različitom ključu. | Docstring na `:1596-1600` izričito tvrdi „isti kanon… tri prikaza se ne mogu razići". Razlaz je vidljiv samo ako se dva taba otvore nad istim nalogom. |
| `work-orders/work-orders.service.ts:1544-1562` | 🔴 | RN je planiran na 10 kom (stavke: 10 pripremaka PLP, 20 vijaka PND). Kontrola knjiži 2 kom škarta ⇒ child `-S1` dobije `pieceCount: 2` u ZAGLAVLJU (`:1549`), ali odmah zatim `cloneItems(..., { coefficient: 1 })` (`:1559`) kopira stavke roditelja **1:1** ⇒ nalog od 2 komada nosi trebovanje za **10 pripremaka i 20 vijaka**. Magacin izda materijal za 10. | Zaglavlje i odštampan RN pišu „Komada: 2" — broj koji čovek gleda je tačan. `bulkClone` u istom fajlu (`:1668`) količine UREDNO skalira koeficijentom, pa odstupanje postoji samo u quality-child grani i deluje kao da je ceo klon-mehanizam ispravan. |
| `handovers/draft-numbering.service.ts:19-21` | 🟡 | Projektant kreira nacrt u 00:30 po Beogradu; Node radi u UTC (v. `common/business-date.ts:5`). `yy` dolazi iz `businessYear` (beogradska godina), a `mm`/`dd` iz `now.getMonth()/getDate()` (**UTC dan**) ⇒ nacrt „od danas" dobije JUČERAŠNJI datum u broju (`G-260731-001` umesto `G-260801-001`). | Broj ostaje jedinstven i formalno ispravan; nesklad izbije tek kad se nacrt traži po datumu ili poredi sa `createdAt`. |

---

## 2. Konkurencija

**Sistemski nalaz iznad pojedinačnih:** `grep -rn "pg_advisory" modules/{tech-processes,handovers,work-orders,pdm,kvalitet,mrp,plan-*,part-locations}` daje pogotke u `work-orders` (4), `handovers` (2), `kvalitet` (1), `part-locations` (2) — i **nula u `tech-processes`**. Najvrelija konkurentna površina u firmi (svaki sken sa svakog kioska i telefona) je jedina bez serijalizacije.

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `tech-processes/tech-processes.service.ts:3524-3549` | 🔴 | Dva kontrolora (ili dupli klik) knjiže po 6 kom na planu 10. Oba `aggregate` vide `existingSum = 0`, oba računaju `cumulative = 6 ≤ 10` ⇒ **nijedan ne dobije overshoot dijalog**, a rezultat je 12. `confirmOvershoot` zaštita se time potpuno zaobiđe. | Overshoot dijalog je jedini mehanizam koji bi upozorio, a upravo on ne okine. Odgovori su uredni. |
| `tech-processes/tech-processes.service.ts:3996-4010` | 🟠 | Komentar na `:3992-3995` tvrdi da je BUG-P2-09 zatvoren „NETO guard"-om, ali guard je isti neserijalizovani `aggregate`. Dva šefa (ili dupli klik) storniraju po 10 na redu od 10: oba pročitaju `netAvailable = 10`, oba prođu ⇒ **neto operacije padne na −10**. | Ispravka je dokumentovana kao rešena, pa se u nju više ne sumnja. Negativan neto se vidi tek na kartici. |
| `tech-processes/tech-processes.service.ts:3734` | 🟠 | `createQualityChildOrder` se poziva POSLE komita kontrole i otvara **sopstvenu** transakciju. Ako padne → `childOrderPending: true` i ručna intervencija. Ako klijent ponovi zahtev zbog timeout-a → druga kontrola + drugi child RN (v. 1.3). Kontrola i njena posledica nisu jedna atomska celina. | Sistem sam prijavi „child nije kreiran, kreirajte ručno" — poruka koja normalizuje neuspeh i sakriva da je uzrok arhitektonski, ne prolazni. |
| `tech-processes/tech-processes.service.ts:4061-4114` (`reopen`) | 🟠 | Tehnolog vraća operaciju u doradu na **legacy** nalogu. `reopen` koristi `tp.workOrderId` (za legacy redove 0) i za `workOrderOperation.updateMany` (`:4097`) i za `workOrder.updateMany` (`:4108`) ⇒ oba pogode 0 redova. Operacija se otvori u `tech_processes`, ali **ostane `priority=255` (van planske table) i nalog i dalje piše „Završen"**. Radnik posao nikad ne vidi. | Odgovor vrati `reopened: 3` (broj otvorenih tp redova) pa akcija izgleda uspešno. `findWorkOrderByTriple` postoji u istom fajlu i rešio bi to, ali se ovde ne koristi. |
| `work-orders/work-orders.service.ts:1269-1280` (`setLock`) | 🟡 | Dva taba: šef otključava RN radi dorade, tehnolog istovremeno zaključava. Bezuslovan `update` — poslednji pobeđuje, bez 409. Sve susedne mutacije (`launch:1170`, `transition:1134`) koriste uslovni `updateMany` baš zbog toga. | Nijedna strana ne dobije grešku; tek sledeća izmena padne na „Zaključan RN se ne može menjati" i deluje kao tuđa greška. |
| `handovers/handover-drafts.service.ts:1505-1517` (`submit`) | 🟡 | Nacrt velikog sklopa (npr. 400 pozicija) → 400 sekvencijalnih `drawingHandover.create` unutar transakcije koja drži `pg_advisory_xact_lock(handover_draft_submit:<id>)`, bez `timeout` (default 5 s). Projektant dobije 500; nacrt ostaje nepredat i neko vreme nedostupan za ponovni pokušaj. | Uspeva na svakom nacrtu normalne veličine; puca samo na najvažnijem (najvećem) sklopu. |
| `tech-processes/tech-processes.service.ts:2853` (`stopWorkById`) | 🟡 | Bilo koji radnik sa `report_work` može poslati `POST /:id/stop-work` za TUĐI `tech_processes` id. `accumulateStopWork` upiše `workerId` = pozivalac (`:3177`) ⇒ **vlasništvo nad redom pređe na njega**, a sa `finishForAll` može ugasiti operaciju i drugima. | Nije bezbednosna rupa (svi su pogonski radnici), ali „ko je radio" na kartici tiho postane pogrešan čovek — a to je ulaz u učinak i u predlog krivca za neusaglašenost (`:3778-3790`). |

**Šta JESTE dobro odbranjeno** (da se ne troši trud dvaput): dupli START je pokriven parcijalnim unique indeksom `uq_work_time_entries_open` (migracija `20260709120000`) uz čist 409; `work-orders.launch` i `handovers.transition/launch/returnToPending` imaju i advisory lock i uslovni `updateMany`; `prepareWorkOrder` ima korektan dvostepeni idempotentni izlaz.

---

## 3. Poslovna rupa

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `work-orders/dto/work-order-operation.dto.ts` + `prisma/schema.prisma` (`WorkOrderOperation`) | 🔴 | **KOOPERACIJA ne postoji kao pojam.** `work_order_operations` nema ni dobavljača, ni datum slanja/povratka, ni status „kod kooperanta". 200 kom ode na kaljenje na 10 dana: operacija je za sistem prosto neotkucana ⇒ `rnProgress` je meri kao 0% (ulazi u `AVG(LEAST(done/plan,1))` kao nula, `:1661`), `critical` (`:1355`) diže RN u crveno svaki dan, a planer nema gde da upiše zašto. | Sistem se ponaša tačno kao da posao kasni — a on ide po planu. Planer to „zna napamet" i ignoriše crveno, čime crveni bedž gubi značenje i za stvarna kašnjenja. |
| `work-orders/work-orders.service.ts:683` (`assertEditable`) + `:922` (`deleteOperation`) | 🔴 | **Izmena TP-a nad pokrenutim nalogom.** Jedini gate je `isLocked`, a „Otključaj" sme svaka `rn.write` rola. Tehnolog otključa lansiran RN i obriše operaciju 30 na kojoj je već otkucano 50 od 100 kom. `deleteOperation` briše samo skice + red operacije — **`tech_processes` kucanja ostaju kao siročići**. Posledica: `rnProgress` ih više ne broji (spaja preko `work_order_operations`) pa gotovost SKOČI; `card` ih i dalje prikazuje; `markWorkOrderIfComplete` ih i dalje broji u `controlledTotal` (spaja preko trojke). Tri ekrana, tri različite istine o istom nalogu. | Brisanje vrati 200 i osvežen RN. Nigde ne piše „na ovoj operaciji ima 50 otkucanih komada — sigurno?". Postoji svest o klasi problema (`BUG-P1-05`, „137 fantomskih redova", `:4466`), ali je rešena samo suprotna smer (sken nad izbačenom operacijom), ne i sam čin brisanja. |
| `tech-processes/tech-processes.service.ts:3581-3592` + `:3641` | 🟠 | **Škart nad već završenim RN-om.** `existingOpen` filtrira `isProcessFinished: { not: true }`, pa se nad zatvorenim nalogom prosto OTVORI nov red i kontrola prođe. `reachedPlan` je odmah `true` (`cumulative ≥ planned`), pa se ponovo pokrene cela kaskada. Child `-S1` nastane, ali `work_orders.status` ostaje `true` i „Gotovost" i dalje 100%. | Naknadni škart je legitiman poslovni događaj i sistem ga uredno primi — samo ga ne odrazi na status naloga. Izgleda kao da je proknjižen. |
| `work-orders/work-orders.service.ts:1055-1063` (`forceRemove`) | 🟠 | `remove` (`:1016`) korektno brani nalog sa evidentiranim radom, ali `forceRemove` briše RN + SVU evidenciju rada + sesije bez ijednog dodatnog uslova i **eksplicitno zaobilazi lock guard** (docstring `:1052`). Nema soft-delete-a ni snapshot-a u `audit_log` — za razliku od `deleteEntry` (`:4140`) koji snima red pre brisanja. Admin greškom obriše RN sa 3 meseca kucanja ⇒ ~40 `tech_processes` redova i sve sesije nestaju nepovratno. | Globalni audit interceptor zabeleži POZIV, ne SADRŽAJ — pa se posle zna ko je i kada, ali ne i šta je bilo. |
| `tech-processes/tech-processes.service.ts:3554-3572` | 🟡 | **Delimično zaduženje.** Parcijalna kontrola je podržana (red ostaje otvoren do plana), ali `part_locations` dobija `+qty` ODMAH ⇒ 30 od 100 kom se vodi kao raspoloživo na polici iako operacija nije zatvorena i kontrola nije završena. | Poslovno je to često tačno (delovi jesu na polici), pa se ne prijavljuje kao greška — sve dok neko ne uzme parcijalu misleći da je lot gotov. |
| `tech-processes/tech-processes.service.ts:3064-3068` (`dismissEntry`) | 🟡 | „Odustani" ne proverava `tp.pieceCount == 0`. Deljeni red ima 8 od 10 kom koje su otkucali drugi; radnik A (jedini sa otvorenom sesijom) pritisne „Odustani" ⇒ red se zatvori na 8/10. `setOperationDonePriority` se NE poziva, pa operacija ostaje na planskoj tabli sa `priority ≠ 255` dok je red zatvoren. | FIX A („ispod-plan = uvek radna", `:4427`) sledećim skenom otvori nov red, pa se sistem sam „izleči" i simptom nestane pre nego što ga neko prijavi. |

---

## 4. 500 umesto jasne poruke

Kontekst: `common/http-exception.filter.ts:47` sve što nije `HttpException` pretvara u `„Neočekivana greška na serveru. Prijavi administratoru šifru greške."` + `traceId`. Za pogon to znači: **svaki P2002/P2028 je za korisnika ista bezizlazna poruka.**

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `work-orders/work-orders.service.ts:1604` (`bulkClone`) | 🔴 | Planer klikne „Kopiraj sve naloge u novi predmet" nad predmetom 7701 (>2.200 RN-a). `$transaction` bez `timeout` → default 5 s; ~10 round-trip-ova po nalogu. Posle 5 s dobije generičku 500, a `pg_advisory_xact_lock(targetProjectId)` je celo to vreme blokirao svaki drugi upis u taj predmet. Ništa ne kaže „predmet je prevelik, radite u paketima". | Radi savršeno na malim predmetima (test-podaci), puca isključivo na velikim — dakle tačno tamo gde je funkcija i potrebna. |
| `work-orders/work-order-numbering.service.ts:73` | 🟠 | Predmet čiji su SVI redovi „otrovni" (svi ordinali ≥ `SANITY_MAX_ORDINAL`, ili nijedan nema prefiks) ⇒ `maxOrd = 0` ⇒ predlog `<broj>/1`. Ako `<broj>/1` sa `variant 0` već postoji, `create` puca na `uq_work_orders_project_ident_variant` ⇒ P2002 ⇒ goli 500. Tehnolog ne može da otvori RN u tom predmetu i ne dobija nijednu uputnicu. | Docstring (`:66-69`) ovaj ishod eksplicitno PRIHVATA („kolizija obara transakciju umesto tihog duplikata") — što je ispravna odluka o podatku, ali je poruka za korisnika izostala. |
| `handovers/handovers.service.ts:1521` i `work-orders/work-orders.service.ts:636` | 🟠 | Isti put: nijedan `workOrder.create` nije obavijen u `catch` koji bi P2002 preveo u 409/422 sa uputnicom. Tehnolog koji „Otkuca TP" na primopredaji u zatrovanom predmetu dobija istu bezimenu 500. | Redak scenario, pa nema testa; kad se desi, prijava glasi „ne radi primopredaja", što vodi istragu u pogrešan modul. |
| `tech-processes/tech-processes.service.ts:2039` (`scan`) i `:3488` (`control`) | 🟠 | Nijedna kioskovska transakcija nema `timeout`. `control` u grani „plan dostignut" radi 4 `updateMany` preko svih redova naloga + `markWorkOrderIfComplete` + N × `partLocation.create`. Na nalogu sa mnogo kucanja i mnogo polica default 5 s je dostižan ⇒ P2028 ⇒ 500 na kiosku, radniku sa porukom „prijavi administratoru šifru greške" i bez ijedne informacije da li je knjiženje prošlo. | Radnik u tom slučaju po pravilu skenira PONOVO — što direktno proizvodi duplo knjiženje iz nalaza 1.3. Dva baga se hrane. |
| `work-orders/work-orders.service.ts:887-919` (`setOperationPriority`) | 🟡 | Orphan operacija (RN ne postoji) ⇒ `wo` je `null` ⇒ `wo?.isLocked` je `undefined` ⇒ lock guard se PRESKOČI i izmena prođe. Nije 500, ali je tiho zaobiđena provera. | Komentar (`:902-903`) to priznaje kao svesno odstupanje; u praksi znači da se prioritet menja i na nalogu koji ne postoji, bez traga. |

---

## 5. Performansa pri rastu

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `work-orders/work-orders.service.ts:1657-1683` | 🔴 | Potvrđen poznati nalaz: petlja bez `take`, ~10 upita po nalogu, pod advisory lock-om, default 5 s. Vidi §4. | — |
| `session-auto-close.service.ts:114-209` | 🟠 | `findMany` visećih sesija **bez `take`**, pa po SVAKOJ sesiji: 1 sy15 upit za kapiju + do 1+2×4 sy15 upita za šefa + 1 mejl + 1 update. Šef se **ne kešira po radniku** (ista osoba se razrešava iznova za svaku svoju sesiju). Sve sinhrono u jednom HTTP zahtevu (`POST /work/auto-close`). Ako cron padne 3 dana ⇒ ~500 sesija × ~10 upita + do 500 pojedinačnih mejlova u jednom zahtevu. | Prolaz normalno obrađuje 5–20 sesija i završi za sekunde. Degradacija je proporcionalna ISPADU crona — dakle stiže tačno kad je sistem već u problemu. |
| `tech-processes/tech-processes.service.ts:4746-4752` (`alignTechProcessSequence`) | 🟠 | `SELECT MAX(id) FROM tech_processes` (98k redova) + `setval` izvršava se **PRE SVAKOG kreiranja reda na kiosku** — svaki prvi sken operacije, svaka kontrola, svaki storno. Isto važi za `alignPartLocationSequence` (`:4734`) po svakoj kontroli. Sa rastom tabele i broja kioska ovo je fiksni trošak na najvrelijoj putanji. | `MAX(id)` ide index-only scan pa je danas jeftin; trošak je nevidljiv dok tabela ne naraste ili dok se broj istovremenih skenova ne poveća. Potreban je samo posle sync uvoza, a plaća se uvek. |
| `work-orders/work-order-numbering.service.ts:46-49` | 🟡 | `findMany` **SVIH** `identNumber` predmeta se prebacuje u Node radi MAX+1, i to **pod advisory lock-om**. Predmet 7701 = 2.273 reda po SVAKOM kreiranju RN-a u tom predmetu; lock drži i sve konkurentne tehnologe. Jedan `$queryRaw` sa `split_part`/regexp u bazi bio bi konstantan i ne bi menjao logiku sanity praga. | 2.273 malena reda su i dalje brzi (~10 ms), pa se ne vidi — ali raste linearno sa najvećim predmetom, i to baš pod lock-om. |
| `tech-processes/tech-processes.service.ts:1583-1698` (`rnProgress`) | 🟡 | 7 koreliranih podupita PO REDU, do 200 redova po strani (`parsePagination` cap), svaki podupit skenira `tech_processes` (98k) — ~1.400 podupitnih izvršenja po strani, plus odvojen `COUNT(*)`. Uz `q` filter dodaje se i `expandSearchWorkOrderIds` (do 500 BOM čvorova + 2×1000 RN-ova). | Ograničeno paginacijom pa nikad ne „visi", samo je svaka strana taba „Gotovost RN" sporija nego što izgleda da bi trebalo. |
| `work-orders/work-orders.service.ts:400-423` (`resolveDrawingIdByNumber`) | 🟡 | Petlja po SVIM revizijama crteža sa `drawingPdf.findFirst` u svakoj iteraciji — klasičan N+1. Poziva se iz `findOne`, dakle **na svakom otvaranju kartice RN-a** koji nema `drawingId` (komentar `:394` kaže da je to 74% CAM pozicija). | Crteži imaju malo revizija (2–5), pa je N mali; primeti se tek na crtežu sa dugom istorijom revizija. |
| `handovers/handover-drafts.service.ts:1495-1500` | 🟡 | Po svakom `submit` se izvršava `GREATEST(MAX(id) drawing_handovers, MAX(drawing_handover_id) work_orders, 9999)` — druga agregacija ide preko `work_orders`. Indeks `idx_work_orders_drawing_handover` postoji pa je jeftino, ali je logika (rezervisanje opsega 10000+) trajno lepljena za svaki submit umesto da bude jednokratna migracija. | Nikad neće biti usko grlo; ubačeno radi potpunosti slike o vrelim putanjama. |

---

## 6. Prateći moduli — isti raspored kategorija

Skraćenice putanja (sve pod `backend\src\modules\`): `KV` = `kvalitet\kvalitet.service.ts` · `QE` = `kvalitet\quality-events.service.ts` · `PL` = `part-locations\part-locations.service.ts` · `MNU` = `montaza-neusaglasenosti\montaza-neusaglasenosti.service.ts` · `PDMI` = `pdm\pdm-import.service.ts` · `PDMS` = `pdm\pdm.service.ts` · `PDMX` = `pdm\pdm-xml-parser.ts` · `PPR` = `plan-proizvodnje\plan-proizvodnje-read.service.ts` · `PPW` = `plan-proizvodnje\plan-proizvodnje.service.ts` · `PM` = `plan-montaze\plan-montaze.service.ts` · `MRP` = `mrp\mrp.service.ts`

### 6.1 Tiho pogrešan podatak

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `PPR:743-744` + `:681-684` | 🔴 | **Ista kolona se čita kao dve različite jedinice.** `work_order_operations.setup_time/cycle_time` se ovde aliasiraju u `tpz_min`/`tk_min` i formula `Tpz + Tk × komada` postaje `effective_duration_minutes` (**MINUTI**). Identična formula nad identičnim kolonama u `work-orders/time-estimate.service.ts:386` nosi alias `AS h` (**SATI**), a `work-orders/work-order-print.service.ts:383-385` je štampa na RN-u. Provereno čitanjem oba upita — obe interpretacije ne mogu biti tačne. Ako je jedinica sat: operacija Tpz 0,5 + Tk 0,2 × 100 kom = 20,5 h posla dobija gant bar od **20 minuta**, i planer nabije dan posla u sat. | FE nosi ISTU konvenciju kao gant (port iz 1.0), pa se BE i FE međusobno potvrđuju. Razilaze se samo od štampe RN-a i obračuna škarta — a te dve stvari niko ne stavlja jedno pored drugog. **Pre ijedne izmene izmeriti nad živim podacima** (jedna operacija poznatog trajanja). |
| `PPR:796` | 🔴 | `komada_done = SUM(t.piece_count)` bez filtera po kvalitetu. RN 100 kom, otkucano 70 dobrih + 30 škarta ⇒ red plana pokazuje **100/100, „preostalo 0"**, a 30 komada tek treba napraviti. Isti lateral (`g4`, `:830-831`) škart već izdvaja posebno — podatak postoji, samo se ne koristi. | Brojka se savršeno poklopi sa planiranom količinom, što izgleda kao uspeh a ne kao greška. Ista klasa greške kao `markWorkOrderIfComplete` u §1 — dakle plan i status naloga greše saglasno. |
| `PPR:731-733` + `:836-844` | 🔴 | `EFF_FILTER` izbacuje SVE preostale operacije RN-a kad je na završnoj kontroli otkucano između 1× i 1,5× količine — a taj zbir uključuje škart. (a) 60 dobrih + 40 škarta = 100 ⇒ **RN nestane iz plana** iako 40 komada fali. (b) RN sa DVE operacije značajne za završetak, obe pune ⇒ 200 > 150 ⇒ uslov nikad ne važi i RN **doveka visi** u redovima. | Nema ni greške ni poruke — red naprosto nije tu, pa planer zaključi da je posao gotov. Obrnut slučaj liči na „zaboravljen RN" pa se krivac traži u pogonu. Isti dvostruki-kontrola scenario obara i `markWorkOrderIfComplete` (§1) — jedan uzrok, dva modula. |
| `work-orders/work-orders.service.ts:1544-1562` | 🔴 | Child RN dorade/škarta nosi materijalne stavke roditelja 1:1 (v. §1, uneto u glavnu tabelu). | — |
| `KV:350` | 🔴 | Izveštaj potvrđen 05.01.2026 dobije `027/26`, `report_year=2026`. Kontrolor ispravi datum na 30.12.2025 ⇒ `reportYear` postane 2025, a `reportNumber` ostane `027/26`. Sledeća potvrda u 2026 računa MAX po (tip, 2026), više ne vidi 27 ⇒ **ponovo dodeli `027/26`**. Dva dokumenta sa istim službenim brojem. | Nema unique indeksa nad (type, report_year, report_number). Lista je sortirana po datumu, K3 grupiše po mesecu — broj nigde nije ključ prikaza. Guard postoji samo za `type` (`:336`). |
| `KV:195`, `:529`, `:690`, `QE:323`, `QE:326` | 🔴 | Rukovodilac traži K3 zbir za jul (`from=2026-07-01&to=2026-07-31`). `parseDateParam` daje `lte: 2026-07-31T00:00:00Z` ⇒ **ceo poslednji dan ispada** iz broja izveštaja, komada, sati i iz Pareto analize. | Broj je uvek uverljiv, samo manji. `montaza-neusaglasenosti.service.ts:192` je isti bug već rešio (`setUTCHours(23,59,59,999)`) — u kvalitetu te korekcije nema nigde. |
| `PDMI:167-193` | 🔴 | Konstruktor izmeni deo pod stanjem „IZMENA BEZ REVIZIJE" (koje parser eksplicitno prihvata, `PDMX:77`) ili revidira DETE dok koren ostane rev B. Ponovni izvoz ima isti koren `(broj, rev)` ⇒ dedup preskoči **CEO FAJL** ⇒ stara sastavnica i stari atributi ostaju u bazi. | Odgovor je `success: true`, log ne-kritičan, poruka „Crtež već postoji — fajl preskočen" zvuči kao normalna idempotencija i u istoriji uvoza izgleda identično kao legitimno ponovno slanje. |
| `PDMI:370-373` | 🔴 | Poređenje revizija je **string**: `others.every(o => normalizeRevision(o.revision) < newRev)`. Uvoz rev `AA` posle `B` → `"B" < "AA"` je false ⇒ nema relinka. Uvoz rev `B` kad postoji `AA` → true ⇒ sve BOM ivice se **prevežu unazad na stariju reviziju**. Numerički isto: `"9" > "10"` ⇒ uvoz „9" gazi „10". | Log kaže „BOM repoint … N komponenti" — brojka je tačna, smer pogrešan. Isti string-MAX koristi i `handovers/handover-drafts.service.ts:953-958`, pa upozorenje „nije poslednja revizija" **potvrdi** pogrešan izbor. Sa A/B/C (99% slučajeva) radi savršeno. |
| `PDMI:306`, `:312-319`, `:336` | 🔴 | `idByDocId` pamti PRVU viđenu reviziju po `docId`. Fajl u kome se isti sklop javlja i kao rev A i kao rev B: sve BOM ivice čiji je roditelj taj sklop zakače se na **rev A**, a delete/recreate obriše komponente rev A ⇒ rev B ostane sa praznom/zastarelom sastavnicom. | `docKey()` i `idByKey` reviziju nose ispravno, pa ceo tok deluje revision-aware. Stats prijave tačan `bomEdgesCreated` — samo na pogrešnom roditelju. |
| `PL:209` | 🟠 | `card()` sabira sve klase kvaliteta na polici: 8 kom DOBAR + 2 kom ŠKART se prikaže kao `{position: A, quantity: 10}`. Monter po kartici uzme 10 i 2 škarta odu u sklop. | `signedBalance()` u ISTOM fajlu (`:436`) namerno filtrira po `qualityTypeId` jer klase nisu zamenljive — kartica to pravilo ignoriše. Ista greška kao `work-orders.resolveLocations` u §1: **dva nezavisna prikaza polica, oba gube kvalitet.** |
| `MRP:338`, `:245` | 🟠 | `freeStock` (zalihe − rezervisano) se lepi na SVAKU potrebu posebno; ništa se ne netuje između potreba. Artikal ima 10 slobodnih; potreba A traži 8, potreba B traži 8 ⇒ **obe pokazuju „slobodno 10"** i obe deluju pokriveno ⇒ 6 komada fali na montaži. | Svaki red je sam za sebe aritmetički tačan; greška postoji samo u zbiru koji nijedan ekran ne prikazuje. |
| `MRP:405-418` + `:99-107` | 🟠 | `mrp_item_stock` puni isključivo legacy QBigTehn sync koji je **mrtav od 22.07**; aplikacija tabelu ne osvežava, a `resolveStock` ne čita ni `updatedAt`. Magacin je juče izdao poslednjih 10 komada — MRP i dalje kaže „slobodno 10" ⇒ nabavka se otkaže. | Broj je prisutan i formatiran isto kao svež podatak; oznake „na dan" nema nigde. |
| `PPR:784-794` + `:709-712` | 🟠 | Prethodna operacija se smatra gotovom ako postoji BILO KOJI `tech_processes` red sa `is_process_finished`, bez obzira na komade. „Kraj rada" na 21 od 200 kom ⇒ sledeća operacija odmah `is_ready_for_machine=true` ⇒ mašina se pripremi za materijal koji ne postoji. | Red izgleda normalno; jedina naznaka je broj komada prethodne operacije, koji red ne prikazuje. (Poznato kao 046-A — ovde je tačna lokacija u kodu.) |
| `PDMI:441-473` | 🟠 | Ponovni uvoz radi `update` sa KOMPLETNIM setom kolona. Izvoz bez `Bb_Kataloski_broj` ⇒ `catalog_number` postane `""`; bez `Naziv` ⇒ `"NEMA PODATAK"`; `statusId` se **uvek** resetuje na 0. Crtež se više ne nalazi pretragom po identbroju (`PDMS:171-174`). | Uvoz javlja „N ažuriranih" — formalno tačno. Nema diff-a ni audita nad `drawings`; polje je prosto prazno i niko ne pamti da je juče imalo vrednost. |
| `PDMI:395-403`, `:421-428` | 🟠 | Relink briše zastarelu ivicu kad par (roditelj, nova rev) već postoji — **količina obrisane ivice se gubi**. Sklop sa 2× P rev A i 1× P rev B posle uvoza ima samo 1× P. | `oldRevisionRelinks` broji brisanja i update-e u jednom zbiru. Posledica ispliva tek u nacrtima kao „tražena količina ≠ PDM sastavnica" i izgleda kao greška tehnologa. |
| `KV:329` | 🟠 | Auto-draft škarta za 5 kom nosi `spentHours=12.5`, `materialKg=47.5`. Kontrolor pri potvrdi ispravi količinu na 2, a sati i kg ostaju za 5 ⇒ K3 prijavljuje 12,5 h za 2 komada. | `recomputeReport()` postoji ali je zasebno dugme koje PATCH ne okida; za doradu (`type=1`) `recompute` vraća 400 (`:477`) — dorada se ne može poravnati uopšte. |
| `PDMS:383-399`, `:537-551` | 🟠 | Poklapanje PDF↔crtež je egzaktno po `(drawing_number, revision)`. Legacy/sync red sa `revision = ''` nikad ne pogodi PDF uvezen kao rev `A`; isto za malo `b` naspram `B` ⇒ trajno `hasPdf: false` i 404. | Komentar tvrdi „obe strane su uvek ne-prazne (uvoz normalizuje)" — što važi samo za nativni uvoz, ne za sync-ovane redove. Filter revizije u listi je `ILIKE`, pa korisnik vidi crtež ali ne i PDF i zaključi da PDF nije poslat. |
| `PPR:782` + `:698-700` | 🟠 | Auto-kooperacija se spaja po ORIGINALNOJ mašini dok sve ostalo radi po efektivnoj. Operacija na koop grupi reassign-ovana na internu mašinu zadrži `is_cooperation_auto=true` ⇒ `OPEN_OPS` je izbaci ⇒ nema je ni u jednom dept tabu, a u tabu „Kooperacija" stoji pod mašinom sa koje je skinuta. | Reassign vrati 200 i audit potvrdi premeštaj; planer traži stavku na novoj mašini i zaključi da je „neko već odradio". |
| `PPR:657` | 🟡 | RN čiji predmet nema red u `predmet_aktivacije` (ili `is_active=false`) nevidljiv je u SVAKOM planerskom pogledu. Nov predmet, RN-ovi lansirani, niko nije čekirao „aktivan" ⇒ plan prazan. | Odsustvo redova nikad ne baca grešku, a modal „TP procedura" (`includeInactivePredmet`) ISTI RN uredno prikaže — pa provera „ima li podataka" ispadne pozitivna. |
| `PL:274` | 🟡 | `POST /part-locations` sa `qualityTypeId: 3` prolazi (`reqNonNegInt` traži samo ≥ 0; kolona nema FK). 5 komada se knjiži u nepostojeću klasu: `card()` ih sabira u ukupno stanje, ali nijedan `requisition`/`transfer` ih ne vidi ⇒ trajno zaglavljeni. | `resolveQualityTypes` vrati `null` pa UI prikaže prazno polje umesto greške, a ukupan broj i dalje „štima". |
| `PDMX:99-111` | 🟡 | `parseWeight("1,5")` → `Number("1,5")` = NaN → **−1**; `parseQuantity("2,5")` → 1. Legacy VBA `CDbl` je locale-aware (srpski zarez) pa je upisivao 1,5. | −1 je dokumentovan „legacy sentinel" pa se čita kao ispravno; u zbiru mase sklopa daje samo nešto manji broj. |

### 6.2 Konkurencija

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `common/db-sequences.ts:16-18` — poziva se iz `tech-processes.service.ts:4737/4750`, `work-orders.service.ts:1892-1907`, `handovers.service.ts:1356/1510`, `PDMI:263`, `PL:267` | 🔴 | **`setval` nije transakcijski i nije MVCC-bezbedan.** Dva kontrolora završe kontrolu istovremeno: obe transakcije rade `setval(seq, MAX(id))`; druga vidi MAX **bez neupisanog reda prve** ⇒ **vraća sekvencu unazad** ⇒ oba `nextval` daju isti id ⇒ drugi INSERT pukne `P2002` na PK ⇒ rollback CELE kontrole (kucanje + lokacije + zatvaranje operacije) uz goli 500. U PDM-u je gore: `PDMI:205-223` hvata **bilo koji** P2002 i prijavljuje ga kao „Crtež već postoji — fajl preskočen", `success: true`, ne-kritičan log ⇒ **fajl tiho nije uvezen**. | Kontrolor ponovo skenira i drugi put prođe — izgleda kao „zakočio se terminal". `setval` se ne rollback-uje pa ne ostavlja trag. Ovo je poravnanje koje treba SAMO posle sync uvoza, a plaća se pred svakim insert-om na najvrelijoj putanji (v. §5). |
| `PPW:63-159` | 🔴 | Drag/resize/Shift+strelice šalju **apsolutne** `plannedStartAt/EndAt` izračunate iz klijentskog snapshot-a, bez verzije i bez `If-Match`; `assertPlanConsistent` proverava samo kraj ≥ početak. Planer A pomeri operaciju +3 dana; planer B, čiji gant nije osvežen, pomeri isti bar za 1 dan ⇒ B upiše `stariPočetak+1` i **tiho poništi A-ov pomeraj**. | Oba dobiju 200, a B na ekranu vidi tačno ono što je očekivao. A sazna tek kad mu se plan „sam vratio". |
| `KV:410` (lock tek na `:419`) | 🟠 | Dupli klik na „Potvrdi": oba zahteva pročitaju `status = 0` **PRE** advisory lock-a. Prvi dobije `027/26` i commit-uje; drugi dobije lock, izračuna MAX+1 = 28 i upiše `028/26` **nad istim izveštajem** ⇒ broj `027/26` je potrošen i zauvek fali u arhivi; oba vrate 200 umesto 409. | Kontrolor vidi jedan izveštaj sa jednim brojem; rupa u sekvenci se otkriva tek pri godišnjem pregledu. Nedostaje `updateMany({where:{id, status: DRAFT}})` — obrazac koji `QE:265` i `MNU:481` već koriste. |
| `PDMI:254-438` (ceo `runUpsert`) | 🟠 | U celom PDM modulu **nema nijednog advisory lock-a**, iako je obrazac ustaljen u repou. Dva paralelna uvoza koja dele podsklop oba rade `deleteMany` + `createMany` nad njegovim komponentama ⇒ poslednji pobeđuje, a **oba** prijave „N BOM veza". | Oba odgovora `success: true` sa nenultim `bomEdgesCreated`. Gubitak se vidi samo poređenjem sa XML-om. |
| `QE:163` | 🟠 | `createByController` nema nikakvu dedup/CAS zaštitu: dupli klik na sporoj mreži ⇒ dva POTVRDJEN događaja po 5 kom istog razloga ⇒ Pareto prikazuje 10 kom i udvostručen udeo. | Dva reda izgledaju kao dve odvojene prijave (različit id, isti `reportedAt`). `confirm()` u istom fajlu (`:265`) IMA ispravan CAS, pa je izostanak u create grani nevidljiv bez poređenja. |
| `PPW:250-271`, `:339-359` | 🟠 | `reorderOverlays` piše pozicije 1..n bezuslovno, bez provere da se klijentska lista poklapa sa stanjem i bez ograničenja na jednu mašinu. Ako A šalje [X,Y] a B [Y,X], transakcije zaključavaju redove obrnutim redosledom ⇒ **PG deadlock** ⇒ jedan dobije 500 i izgubi ceo redosled. Do 2000 stavki × jedan upsert, u default 5 s transakciji. | Nema sukoba na ekranu — lista se posle refresh-a „vratila", što liči na grešku u prevlačenju. |
| `MNU:437` | 🟠 | `updateInvestigation` nema CAS (za razliku od `changeStatus` na `:481`) i **ne proverava status uopšte** — prijava u ZAVRSENO se sme prepisati, iako `addPhotos` (`:320`) ZAVRSENO blokira. Dva menadžera ⇒ poslednji tiho pregazi prvog. | Upiše se `INVESTIGATION_UPDATED` event pa izgleda kao uredna izmena; podnosilac je već dobio mejl sa starim zaključkom i ne dobija novi. |
| `PM:491-518`, `:361-382`, `:417-445` | 🟠 | `checks` faze se piše kao ceo 8-člani niz ⇒ dva inženjera na istoj fazi: ko snimi drugi briše tuđi čekiran kvadratić. Isto za POST sa klijentskim `id` = upsert celog zapisa bez provere svežine ⇒ forma otvorena 10 minuta vraća stara polja preko tuđih izmena. | Klasičan lost update — obe strane vide „sačuvano". |

### 6.3 Poslovna rupa

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `KV:444` + `dto/update-nonconformity-report.dto.ts:55` | 🔴 | **Potvrđen izveštaj o neusaglašenosti se ne može stornirati.** `DELETE` → 422, `PATCH quantity: 0` → 400 (`quantity` mora biti ≥ 1), a `STATUS` poznaje samo 0/1 — nema STORNIRAN. Pogrešnih 5 komada zauvek ostaju u K3, u `summaryByWorker` pripisani nevinim radnicima i u njihovom „Moje neusaglašenosti". | Jedini izlaz je prepisati `workOrderId`/`identNumber` na tačan nalog — greška postane nevidljiva ali ostaje knjižena, a nad `nonconformity_reports` nema audita da se vidi šta je bilo pre. |
| `PDMI:363-437` | 🔴 | Relink preusmerava SVE BOM ivice na novu reviziju **bez ijedne provere da li je stara vezana za pokrenut RN ili predatu primopredaju**. RN je pušten po sklopu sa P rev A; stigne P rev B; ivica se prepiše ⇒ `whereUsed(P rev A)` postane **prazno** („nigde se ne koristi") dok se u pogonu upravo prave komadi po njemu, a operateru se nudi PDF rev B. | Nema audita nad `drawing_components` (update in-place) — stanje pre relinka je nepovratno. RN i dalje čuva `drawing_id` stare revizije pa izgleda ispravno; nesklad se vidi samo ako neko uporedi RN i sastavnicu. Isti problem kao brisanje operacije nad pokrenutim nalogom u §3 — **konstrukcija i tehnologija oboje smeju da menjaju podlogu pod pokrenutim poslom.** |
| `PPW:76-78` + `dto/plan-proizvodnje-mutation.dto.ts:42` | 🔴 | `assignedMachineCode` se upisuje direktno kroz `POST /overlays` sa samo `plan_proizvodnje.edit` — **zaobilazi CEO reassign gate** (`PPW:396-419`: grupa → 422, force → 403 + obavezan razlog, audit red). Planer bez `.force` prebaci strugarsku operaciju na glodalicu bez razloga i bez traga. Vrednost je slobodan string bez provere postojanja ⇒ tipfeler i operacija padne u „Ostalo" i nestane iz svog dept taba. | Tab „Audit forsiranih premeštaja" ostane prazan — što se čita kao „niko nije forsirao", a ne kao „gate je zaobiđen". |
| `PDMI:310-323` | 🟠 | Povlačenje pozicije iz sastavnice radi samo ako dokument u tom fajlu ima `<references>` i ako fajl prođe root dedup. Ukloni li se pozicija bez promene revizije korena, fajl se preskoči i **obrisana pozicija zauvek ostaje u sastavnici** ⇒ nabavka i plan za deo izbačen iz konstrukcije. | Uklonjena pozicija i dalje ima validan crtež i PDF, pa ništa ne izgleda pokvareno — samo je višak. |
| `PM:417-477` | 🟠 | Semantika `?? undefined`: **null nikad ne briše polje.** Planer skine montažera/lokaciju/rnCode i snimi — stara vrednost se vrati. Datumi (`toDbDate`) SE brišu, pa se pola forme ponaša jedno, pola drugo. | Snimanje vrati 200 i `{id}`, a ne sam zapis; ekran se osveži tek sledećim učitavanjem, kad se „vraćena" vrednost pripiše tuđoj izmeni. |
| `PPW:89-105` | 🟠 | `cooperationStatus` je nevalidiran string iako je dozvoljeni skup u šemi (`schema.prisma:3018`). Bilo koja druga vrednost čini `cooperation_status <> 'none'` tačnim ⇒ operacija se tretira kao u kooperaciji i **nestaje iz svih otvorenih redova**. | Nestanak jedne stavke iz reda mašine niko ne poveže sa poljem statusa kooperacije. |
| `MRP` (ceo modul) | 🟡 | Nema delimične pokrivenosti: modul izlaže `requiredQuantity`/`reservedQuantity`/`toProcureQuantity`, ali manjak (`required − reserved − toProcure`) ne računa nijedan endpoint, iako je to uslov spremnosti iz specifikacije. Planer ručno oduzima po stavci. | Sve tri kolone su tu i tačne — ekran izgleda potpun. |
| `PPW:209-218` | 🟡 | Od ciklusa u uslovima hvata se samo samoreferenca (dužina 1); A→B i B→A prolazi. Sam komentar (`:172-174`) kaže da bi se „F2 auto-pomeranje po uslovu" na tome zavrtelo. | Upis prođe sa 200; posledica se javi tek kad se auto-pomeranje uključi. |

### 6.4 500 umesto jasne poruke

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `KV:356`, `:287-305` | 🟠 | Kontrolor prekopira „Radnu jedinicu" iz Excela (`work_unit VarChar(120)`) ⇒ `value too long for type character varying(120)` kao goli 500; cela izmena kartice (sa ispravljenim satima i uzrokom) se izgubi. Nijedno string polje se ne klipuje, DTO ne proverava dužine. | Modul je eksplicitno digitalizacija Excel evidencije — **paste iz Excela JE primarni tok rada**. `uploadDocument` u istom fajlu (`:978`) koristi `clip()`, a montaža klipuje svih 5 polja, pa nedoslednost nije očigledna. |
| `KV:422` | 🟠 | Uvoz Excel istorije (koji docstring na `:405` sam najavljuje) upiše `report_number = ''` ili `'12a/26'`. Od tada SVAKA potvrda za taj (tip, godina) puca `22P02 invalid input syntax for type integer` ⇒ 500. Filter je samo `report_number IS NOT NULL` — prazan string prolazi. | Greška izbije tek pri prvoj potvrdi POSLE uvoza, u drugom modulu nego što je uvoz rađen; prijava glasi „ne mogu da potvrdim izveštaj". |
| `PDMX:295-300` | 🟠 | Ključ za „četvrti uslov" je `${marking}|${revision}|${parentRowIndex}` — po **Oznaci**, ne po `docId`. Dva različita kupovna dela pod istim sklopom sa istom oznakom (`DIN912`, `SRPS`…) ⇒ **ceo fajl odbijen**, ništa se ne upiše, a poruka imenuje dokument koji nije duplikat. | Poruka je formalno tačna po svom kriterijumu i zvuči kao greška konstruktora u CAD-u, pa se traži nepostojeći duplikat u modelu umesto da se ospori pravilo. |
| `PDMI:141` | 🟠 | Sadržaj se UVEK dekodira kao UTF-8. XML u windows-1250 prođe parser, ali svako `š/đ/č/ž` postane `U+FFFD` ⇒ nazivi i materijali se tiho upišu iskvareni. UTF-16 ⇒ `PdmXmlStructureError` „XML nije dobro struktuiran", što upućuje na pogrešan uzrok. | Uvoz je `success: true`, brojke tačne; kvar je vidljiv tek kao „�" u nazivu i pripiše se fontu ili pregledaču. |
| `PDMI:120-123` | 🟠 | Bridge pokupi fajl koji CAD još upisuje i pošalje **0 bajtova** ⇒ 400 „Nedostaje XML fajl" i **nijedan red u `drawing_import_log`**. Isto za fajl preko multer limita (413, engleski, bez loga). | Sve odbijeno PRE ulaska u telo servisa ne postoji u istoriji uvoza — fajl izgleda kao da nikad nije ni pokušan. |
| `dto/plan-montaze-mutation.dto.ts:130` + `PM:957-961` | 🟠 | `datum` je samo `@IsString()` (svi ostali datumi su `@IsISO8601()`), a `toDbDate` seče 10 znakova i radi `new Date(...)`. AI izlaz je **DD.MM.YYYY** ⇒ Invalid Date ⇒ Prisma ⇒ 500. Web wizard konvertuje, pa je zaštićen samo taj put. | Monter sa terena vidi „greška pri čuvanju"; tekst izveštaja i fotke ostaju samo na telefonu. |
| `PM:686-718` | 🟠 | Fajlovi idu u storage pre meta redova. Ako `createMany` padne: 500, objekti ostaju siročići u bucket-u, a sračunato `uploaded/failed` se baci. `redniBroj` nema unique, a početak se računa kao `count()` postojećih ⇒ **ponovno slanje svih fotki posle delimičnog pada duplira već otpremljene**. | Izveštaj posle toga ima 5 fotki umesto 3, a monter je video samo crvenu poruku i poslao ponovo. |
| `PPW:384-386` | 🟠 | Operacija sa praznim `work_center_code` ⇒ `original = NULL` ⇒ 422 **`operation_not_found`**, iako operacija postoji. Planer je premesti sa „bez mašine" na mašinu i dobije poruku da linija ne postoji. | Poruka je uverljiva — planer pomisli da je RN obrisan i traži ga u drugom modulu. |
| `QE:171` | 🟡 | `unit` ide direktno u `VarChar(10)` a DTO ga ne validira: „kom. kompleta" (13) ⇒ 500. | Default „kom" pokriva skoro sve; otkriva se tek kad neko kuca u kg ili metrima. `workUnitCode` u istoj metodi JESTE odsečen. |

### 6.5 Performansa

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `PPR:164-167` | 🔴 | Tab „Sve" ima `LIMIT 5000` **bez ikakvog `truncated` u odgovoru** (`operations()` vraća samo `{data}`), a na produ je izmereno ~16k otvorenih operacija ⇒ sve iza reza je nevidljivo, bez trake upozorenja. Gant (`:296-304`) ima isti limit i JESTE vraća `meta.truncated`, ali sortira po `hall ASC` ⇒ hale pri kraju abecede se renderuju kao **prazne grupe**. | Lista je puna i uredno sortirana; nedostaju stavke sa najdaljim rokom koje niko ne traži danas. Prazna „Hala 3" izgleda kao uverljivo stanje „ništa nije planirano". |
| `PDMI:364-367`, `:386-403`, `:412-428` | 🟠 | Po SVAKOM upsert-ovanom dokumentu jedan `findMany` starih revizija (uvek, i kad ih nema), pa po SVAKOJ zastareloj ivici `findFirst` + `update`/`delete`. Standardni deo u 200 sklopova = 400 round-tripa za jedan crtež; fajl sa 20 takvih ≈ **8000 upita u jednoj transakciji**. `timeout: 120_000` je zavoj — kad se probije, ceo fajl se rollback-uje. | Timeout od 2 minuta pokriva sve dosadašnje fajlove; probije se tek kad broj deljenih delova poraste. |
| `PPR:165` | 🟠 | Dept put zove `effectiveOpsInner(Prisma.empty)` — filter odeljenja se primenjuje TEK u spoljnom `WHERE`, pa svih 7 laterala radi nad SVIM aktivnim RN-ovima pre nego što se odseče odeljenje. Komentar na `:637-640` izričito kaže da `baseFilter` postoji baš da se to izbegne — ali ga koristi samo `?machine=`. | Odgovor je tačan, samo skup; razlika je merljiva jedino profilisanjem upita. |
| `PPR:220-235` | 🟠 | `operationsAll` računa celu izvedenu tabelu sa 7 koreliranih laterala **dva puta** po pozivu (redovi + count); ista računica je izmerena na ~5,3 s. | Ekran se učita „malo sporije"; duplo opterećenje vidi se samo u pg statistici. |
| `PDMS:701-734` | 🟠 | Rekurzivni CTE za sastavnicu **nema cap na broj redova**, a default dubina je MAX (20) kad `?depth` nije poslat. CTE nabraja svaku PUTANJU, ne čvorove — a PDM podaci su baš oblika „isto podstablo pod 6 roditelja" ⇒ desetine hiljada redova i isto toliko ključeva u `nodeByPath` Map-i. | Anti-ciklus guard postoji i radi, pa deluje kao da je stablo ograničeno; eksponencijalno grananje BEZ ciklusa nije pokriveno nikakvim limitom. |
| `PDMS:438-481` | 🟠 | `resolveWorkOrderRefs` vuče SVE RN-ove koji pogađaju brojeve crteža, bez `take`, uz `mode: insensitive` na `in` (ne koristi indeks), pa lokalno radi ugnježdenu petlju `stranica × svi RN-ovi`. Poziva se na SVAKOM listanju i detalju. | Kolona „RN" je nova i na malim stranicama brza; degradira sa rastom `work_orders`, a EXPLAIN niko ne gleda jer je upit „samo lookup". |
| `PM:96-137` | 🟠 | `projectsTree`: `pb_list_projects()` + SVI work packages + SVE faze bez `take`, unutar `withUserRls` transakcije sa 5 s limitom ⇒ rast broja projekata ⇒ P2028 ⇒ 500 na početnom ekranu montaže. | Dok ne pukne, samo je sporo; kad pukne, ruši ceo ekran a ne jedan red. |
| `KV:874` | 🟡 | `mine()` materijalizuje SVE `reportId` radnika u JS niz i vraća ih kao `{ in: [...] }` bind-parametre, iako uzima samo 50 redova — tačno obrazac koji je u `PL:95` već obarao upit preko PG limita od 65535 bind-parametara i zato bio prepravljen na `EXISTS`. | Radi dok radnik ne nakupi mnogo zapisa; do tada je stranica brza. |
| `PPR:195-217` | 🟡 | `machineOps` nema `LIMIT` u SQL-u uopšte — materijalizuje sve otvorene operacije mašine i paginira po RN-u u JS-u. | Za većinu mašina skup je mali; javi se samo na najopterećenijoj mašini. |

---

## TOP 10 za ovaj domen

| # | nalaz | fajl:linija | ozb. | trud |
|---|---|---|---|---|
| 1 | **`setval` poravnanje sekvence je samo po sebi race** — nije transakcijsko, druga transakcija vidi `MAX(id)` bez tuđeg neupisanog reda i **vraća sekvencu unazad** ⇒ P2002 na PK ⇒ rollback cele kontrole; u PDM-u ga catch prijavi kao „već postoji, preskočeno" pa se **fajl tiho izgubi**. Radi se pred SVAKIM insert-om na najvrelijim putanjama, a potreban je samo posle sync uvoza. Lek: `pg_advisory_xact_lock` po tabeli kao prvi statement (obrazac već u repou), ili — bolje — poravnanje jednokratno u migraciji i izbacivanje iz runtime-a; usput suziti PDM catch na `error.meta.target` koji sadrži `drawing_number`. | `common/db-sequences.ts:16-18` + 10 pozivnih mesta (`tech-processes:4737/4750`, `PDMI:263`, `PL:267`, …) | 🔴 | **M** |
| 2 | **Tvrdi guard „preko plana" je TOCTOU, na sva tri ulaza** — dve paralelne prijave/kontrole probijaju plan bez poruke i bez overshoot dijaloga (8+8 na planu 10). Lek: `SELECT … FOR UPDATE` na redu operacije (ili advisory lock po petorci) PRE `aggregate`, u `scan`, `accumulateStopWork`, `control` i `storno`. | `tech-processes.service.ts:1952`, `:3524`, `:3996` | 🔴 | **M** |
| 3 | **Kontrola nije idempotentna, a `tech_processes` nema unique na petorci** — dupli klik daje duple police, duple komade i **dva child RN-a za isti škart**; istovremeni prvi sken otvara dva reda za istu operaciju pa se RN posle nikad ne zatvori. Nalaz #8 (500 na kiosku) baš i izaziva ponovni sken. Lek: parcijalni unique indeks (obrazac `uq_work_time_entries_open` već postoji) + advisory lock po `(trojka, operacija)` + child RN u ISTOJ transakciji. | `schema.prisma:1983`, `tech-processes.service.ts:4418`, `:3524-3572`, `:3734` | 🔴 | **L** |
| 4 | **Storno ne poništava ništa osim komada** — polica (`part_locations`), `work_orders.status`, prioritet operacije i child `-D/-S` RN ostaju živi; a potvrđen izveštaj o neusaglašenosti se **uopšte ne može stornirati** (DELETE 422, `quantity: 0` 400, nema statusa STORNIRAN). Jedan klik koji izgleda kao ispravka trajno razilazi četiri evidencije. Lek: kompenzacioni tok kroz 3 modula + status STORNIRAN — traži presudu vlasnika šta ide automatski a šta ručno. | `tech-processes.service.ts:3982-4051`, `KV:444` | 🔴 | **L** |
| 5 | **Škart se svuda broji kao „urađeno"** — `markWorkOrderIfComplete` (`:4831`) digne „Završen" i kad je svih 100 kom škart; `PPR:796` prikaže 100/100 „preostalo 0"; `PPR:731` sakrije ceo RN iz plana. Isti kod usput sabira preko RAZLIČITIH operacija (2×60 = 120 ≥ 100) i razilazi se sa `rnProgress` koji spaja po drugom ključu. Lek: jedna funkcija/pogled „napravljeno DOBRO po operaciji", MIN po operaciji, jedan ključ spajanja — pa je pozvati sa sva tri mesta. | `tech-processes.service.ts:4831`, `:1601`, `PPR:796`, `PPR:731-733` | 🔴 | **L** |
| 6 | **Child RN dorade/škarta nosi materijalne stavke roditelja** — zaglavlje kaže 2 komada, trebovanje traži materijal za 10; `bulkClone` u istom fajlu količine uredno skalira. Lek: `coefficient = quantity / source.pieceCount` (uz potvrdu vlasnika za zaokruživanje; norme operacija se namerno ne skaliraju). | `work-orders.service.ts:1544-1562` | 🔴 | **M** |
| 7 | **Tpz/Tk: ista kolona, dve jedinice** — gant je čita kao minute (`tpz_min`, `effective_duration_minutes`), procena i štampa RN-a kao sate (`AS h`). Jedna od dve strane greši 60×. Lek: **prvo izmeriti** nad jednom operacijom poznatog trajanja, pa uskladiti jednu stranu, preimenovati kolone da ime nosi jedinicu i migrirati postojeće `planned_duration_minutes`. | `PPR:743-744`, `:681-684` vs `time-estimate.service.ts:386`, `work-order-print.service.ts:383` | 🔴 | **M** |
| 8 | **Noćna smena = 0 minuta + optužujući mejl šefu** — kapijski upit traži izlaz na kalendarski dan STARTA, pa utorkov izlaz u 06:05 za ponedeljak-22:10 sesiju ne postoji. Uz to `closeSession` nema `where stoppedAt: null` pa gazi sesiju koju je radnik u međuvremenu uredno zatvorio. Lek: prvi `direction='out'` POSLE `started_at` u prozoru ~20 h bez uslova na datum + uslovni update. | `session-auto-close.service.ts:263-273`, `:246-249` | 🔴 | **S** |
| 9 | **Podloga pod pokrenutim poslom se sme menjati bez traga** — tehnolog otključa lansiran RN i obriše operaciju sa 50 otkucanih komada (kucanja ostaju siročići, tri ekrana se raziđu); PDM relink prepiše BOM ivicu na novu reviziju dok se po staroj upravo radi (`whereUsed` stare revizije postane prazan, nema audita). Lek: 422 kad na operaciji postoji ijedno kucanje i kad je crtež u pokrenutom RN-u — uz eksplicitan force sa razlogom i snapshot-om. | `work-orders.service.ts:683`, `:922`; `PDMI:363-437` | 🔴 | **M** |
| 10 | **Tihi 500 na kiosku i tiho sečenje planerskih listi** — kioskovske transakcije nemaju `timeout` pa P2028/P2002 stižu radniku kao „prijavi administratoru šifru greške" (i on skenira ponovo → nalaz #3); `bulkClone` na velikom predmetu sigurno ističe uz držan lock; tab „Sve" seče 16k operacija na 5000 **bez ijedne oznake**. Lek: eksplicitni `timeout`-i, mapiranje P2002/P2028 u 409 sa srpskom porukom, i `meta.total/truncated` na svakoj listi koja seče. | `tech-processes.service.ts:2039`, `:3488`; `work-orders.service.ts:1604`; `PPR:164-167`; `http-exception.filter.ts:47` | 🔴 | **M** |

**Trud:** S = do pola dana · M = 1–3 dana · L = nedelja+ (traži odluku vlasnika procesa)

**Najjeftinije popravke van TOP 10** (svaka ≈ S, svaka sama za sebe zatvara stvarnu grešku u brojkama):
`KV:195/529/690` + `QE:323/326` — filter `to` seče ceo poslednji dan u SVIM izveštajima kvaliteta; korekcija već postoji u `montaza-neusaglasenosti.service.ts:192`, treba je izvući u `common/date-params.ts` i pozvati na 5 mesta · `KV:410` — `confirmReport` čita status pre lock-a, dodati `updateMany({where:{id, status: DRAFT}})` + 409 · `KV:350` — blokirati promenu `reportDate` na potvrđenom izveštaju + unique (type, report_year, report_number) · `PPW:76-78` — izbaciti `assignedMachineCode` iz overlay patch-a da reassign gate ostane jedini put · `PDMI:441-473` — upisivati samo atribute prisutne u XML-u i ne resetovati `statusId`.

---
