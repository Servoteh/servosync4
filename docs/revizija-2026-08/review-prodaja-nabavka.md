# Dubinski funkcionalni review — PRODAJA + NABAVKA

Baza koda: `C:\Users\nenad.jarakovic\wt\robno-quality\backend\src`
Obuhvat: `modules/sales/**`, `modules/nabavka/**` (`modules/robno/**` NIJE diran).
Datum: 04.08.2026.

Modul je već prošao više krugova pregleda i to se vidi — zaokruživanje PDV-a, „ILI-ILI" avansa,
CAS-claim pri knjiženju i SEF `CANCEL_PENDING` su zatvoreni i dobro obrazloženi. Nalazi ispod su
ono što je **ostalo ispod tih krugova**: uglavnom stvari koje nijedan od dosadašnjih krugova nije
gađao — valuta, promena zaglavlja nacrta, idempotencija SEF reda, i ceo tok nabavke posle prijema.

Zbir: **43 nalaza** — 🔴 8 · 🟠 21 · 🟡 14.

---

## 1. Tiho pogrešan iznos na fakturi

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `sales/fakturisanje.service.ts:1521-1548` (+ `sales/advance-invoice.service.ts:577-620`, `890-933`) | 🔴 | Knjižiš IZVUS na **10.000 EUR** (kurs 117,20). GL dobija `2050 DUG 10.000,00 / 6140 POT 10.000,00`, `currency='EUR'`, a `fxDebit/fxCredit/fxCurrency` (`schema.prisma:3488-3490`) ostaju NULL. Umesto 1.172.000 RSD potraživanje i prihod su **117× manji**. Isto važi za naplatu i odbijanje avansa u EUR. `invoice.exchangeRate`/`accountingExchangeRate` u celom modulu prodaje ne čita **nijedan** pisač GL-a (grep: samo prepis kolone u `carry-over` i AVR). | Nalog BALANSIRA (ista pogrešna suma s obe strane), pa balans-kontrola (`:1508`) ćuti. `assertTotalsMatchItems` poredi zaglavlje sa stavkama — obe strane su u EUR, dakle „slažu se". Papir i SEF ispisuju EUR i izgledaju tačno. FX revalorizacija tu grupu odbaci kao `NO_FX_PAIR` (`saldakonti/fx-revaluation.service.ts`), pa devizna stavka i **tiho ispadne** iz jedine kontrole koja bi je uhvatila. Izvozna faktura ne ide ni na SEF, pa druge kontrole nema. |
| `sales/sales.service.ts:746-748` + `sales/dto/update-invoice.dto.ts:218-225` | 🔴 | Predračun otkucan u RSD (120.000,00). Operater u zaglavlju promeni valutu na `EUR` (`PATCH /sales/documents/:id`). Nijedan iznos se ne dira — dokument sada glasi **120.000,00 EUR**. Ni `exchangeRate` se ne traži uz promenu, ni upozorenja nema. | `recalcTotals` se pozove i vrati **iste** brojeve (računa iz stavki, valutu ne poznaje), pa izgleda kao da je preračun izvršen. Na papiru se promeni samo oznaka valute pored istog broja. |
| `sales/sales.service.ts:203-211, 745` | 🔴 | Predračun napravljen za kupca A (rabat 25 %). Operater ispravi kupca na B (bez rabata) kroz izmenu zaglavlja. Stavke ostaju sa 25 % rabata i cenom kupca A — B dobija tuđu rabatnu politiku. Obrnuto isto: kupac sa ugovorenim rabatom dobija pune cene. | `PricingService.priceItem` se u toj grani **nikad ne zove** — `headerUpdateData` (`:733-764`) samo upiše `customerId`, a `applyCoefficient` se okida isključivo uz `priceCoefficient`. Ekran pokaže novog kupca i nepromenjen iznos, što izgleda kao „samo sam ispravio komitenta". |
| `sales/fakturisanje.service.ts` — nema ijedne provere naplaćenog AVR-a pri `postInvoice` (`:600-867`) | 🔴 | PROF 44 → AVR `A-2/26` na 120.000, naplaćen (`2040 DUG 120.000`). Isti PROF 44 → prepis u IFR → knjiženje na **punih 120.000** (`2040 DUG` još 120.000). Kupac u saldakontima duguje 240.000 i dobija fakturu na pun iznos. Odbijanje avansa je zasebna ručna radnja (`POST /invoices/:id/apply-advance`) bez ijednog podsetnika. | Oba dokumenta su tehnički ispravna i oba prođu sve brane. `computePayableAmount` na IFR-u vrati pun bruto, jer primene nema — „za uplatu" je tačno po pravilu, a pogrešno po poslu. Isplivava tek kad kupac odbije da plati drugi put. |
| `sales/advance-invoice.service.ts:679-746, 812-833` | 🟠 | AVR `A-3/26` naplaćen **5.000 EUR**; konačni račun IFR `120/26` na **600.000 RSD**. `applyAdvance` prođe sve brane (isti kupac, izlazni smer, naplaćen) i nominalno odbije 5.000 → „za uplatu 595.000". GL dobije `4300 DUG 4.166,67 / kupac POT 5.000` sa `currency` konačnog računa. | Nijedna od pet kontrola iznosa (`:791-833`) ne poredi valutu — samo brojeve. 5.000 na računu od 600.000 izgleda kao normalan delimičan avans. |
| `sales/advance-invoice.service.ts:1067-1094` | 🟠 | Predračun 1.000.000 po 20 % + 900.000 po 10 %. Avans 1.000.000 → cela osnovica po **dominantnoj** stopi 20 % (PDV 166.666,67), iako polovina prometa ide po 10 %. PDV prijava meseca naplate je preplaćena po višoj stopi, a „ostatak se poravnava na konačnom računu" (komentar `:1069-1070`) u kodu ne postoji kao radnja. | Samo `logger.warn` (`:1080`). Dokument je interno dosledan, bruto je tačan, papir i e-faktura se slažu — pogrešna je samo poreska kolona POPDV-a. |
| `sales/advance-invoice.service.ts:1073` | 🟠 | `resolveVatRateCode` na dokumentu **bez stavki** vraća `"3"` = 20 %, bez upozorenja (`items.length===0` grana je PRE provere `byCode.size > 1`). AVR po ugovoru za oslobođen promet tiho dobija 20 % preračunate stope. | Grana koja upozorava na mešovitu stopu se preskoči; svuda drugde u fajlu nepoznata šifra pada sa 422 (`:1057`), pa se pretpostavlja da i ovde pada. |
| `sales/fakturisanje.service.ts:838-845` | 🟠 | Knjiženje čeka bravu numeracije; u tom prozoru drugi operater unese datum prometa (`PATCH` prolazi — CAS još nije odradio). Korak 3b čita **zastareli** `invoice.supplyDate` (snapshot spolja), ne `fresh` (`:733`) koji je upravo pročitan pod bravom — pa upiše datum izdavanja i zaloguje „nema unet datum prometa". | Ishod je validan datum na dokumentu i uredna WARN poruka. Razlika se vidi samo poređenjem sa onim što je operater uneo. Isti razlog zbog kog je `fresh` uveden nije primenjen na ovaj korak. |
| `sales/carry-over.service.ts:219-221` | 🟡 | Predračun od 15.01. sa rokom plaćanja 30.01. Prepis 20.02. → račun nosi `documentDate = danas` (20.02.), ali `dueDate` NASLEĐUJE (30.01.). Račun je dospeo **pre nego što je izdat**. `kamata.service.ts` i priprema plaćanja rade po `dueDate`. | Oba polja su popunjena i izgledaju uneta; niko ne poredi njihov međusobni redosled. Nema validacije `dueDate >= documentDate` nigde u modulu. |
| `sales/advance-invoice.service.ts:1119-1125` | 🟡 | Zatečena kolona `advance_applied_amount` veća od bruta (uvoz BigBit istorije — jedna faktura zatvara 6 avansa, ručna ispravka u bazi) → `computePayableAmount` odseče na `0` i višak nestaje bez traga. | „Za uplatu 0,00" je legitiman ishod punog avansa, pa se ne razlikuje od greške u podacima. |
| `sales/carry-over.service.ts:118-122` + nema rute za brisanje nacrta (`sales.controller.ts` — samo `DELETE …/items/:lineId`) | 🟡 | PROF 44 prepisan u **IFR** umesto IFUSL. Nacrt IFR se ne može ni obrisati (nema rute; grep `invoice.delete` = 0 pogodaka) ni stornirati (storno traži `isLocked`, `:921`). Ponovni prepis daje 409 „već prepisan u račun N". Jedini izlaz je ručna izmena baze. | Anti-duplo guard je namerno strog i dobro obrazložen; da nema izlaza otkriva se tek kad neko pogreši vrstu. |

---

## 2. SEF (e-faktura)

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `sales/sef/sef.service.ts:125-375` + `sef/sef.controller.ts:137-148` | 🔴 | `enqueue` **nema anti-duplo guard**. Dva klika na „Pošalji na SEF" (ili dva korisnika) daju dva outbox reda, oba `PENDING`, svaki sa **svojim** `requestId`. Oba prolaze `send()` — uslov je samo `status === 'PENDING'` (`:407`). SEF dobija fakturu `657/26` **dvaput**, kao dva nezavisna zahteva. Kupac ima dve važeće e-fakture na isti broj. | Zaglavlje fajla tvrdi „IDEMPOTENCIJA: `requestId = crypto.randomUUID()` po outbox redu; SEF deduplira slanje po njemu" (`:36-38`) — ali UUID **po redu** znači da su dva reda dva različita zahteva, pa dedup po `requestId` po konstrukciji ne može da radi. Pooštrenje `send()` na `PENGING`-only (nalaz N5, 03.08.) je usput učinilo drugi red **bezbedno slanjivim** umesto da ga zaustavi. Lista `/sef/outbox` prikazuje oba reda kao normalna. |
| `sales/sef/sef.service.ts:137-141` | 🟠 | Račun je storniran (`cancelSefOutbox` je već ugasio sve njegove redove). Neko posle toga pozove enqueue → nastane **nov `PENDING` red za `CANCELLED` fakturu**. Guard proverava `level !== 0 \|\| status === 'DRAFT'`, a `CANCELLED` nije nijedno od toga. | `send()` ga odbija tek pri slanju (`:421-431`), pa greška izgleda kao problem trenutka. Red do tada trajno stoji u redu za slanje na ekranu /sef i u svakom pregledu „čeka slanje". |
| `sales/sef/sef.service.ts:553-609` | 🟠 | Kupac odbije e-fakturu na portalu → poll upiše `REJECTED` u outbox i tu se sve zaustavi. Faktura ostaje `POSTED`, u KIF-u, u saldakontima i u obračunu kreditnog limita. Nema ni mejla, ni zadatka, ni oznake na samom računu. Sledeći `enqueue` napravi nov red bez ikakvog traga da je prethodni odbijen. | Status je vidljiv jedino onome ko otvori /sef i pogleda baš taj red. Nijedan izveštaj prodaje ne spaja stanje SEF-a sa stanjem računa. |
| `sales/sef/sef.service.ts:583-595` | 🟠 | Red je pao sa greškom (`errorMessage` postavljen, status `PENDING`). Poll vrati status koji `mapSefStatus` ne prepoznaje → `localStatus = null` → `status: undefined` (ostaje `PENDING`), **ali `errorMessage: null`** — poruka o grešci se briše. | Red posle toga izgleda kao uredan `PENDING` bez ijednog problema; razlog zbog kog stoji u redu je obrisan. |
| `sales/fakturisanje.service.ts:1356, 1362` + `sef/sef.service.ts:879-906` | 🟠 | Storno čita outbox sa `take: 200`, a `listOutbox` sortira `id: desc`. Dokument sa više od 200 redova (dostižno preko nalaza iznad) → otkazuju se samo **najnoviji** 200, a najstariji `SENT` redovi — baš oni koji su stvarno stigli kupcu — ostaju živi. | Storno vraća `sefCancelledOutboxIds` sa 200 uspešnih otkazivanja i nijednom greškom; ekran javi „Otkazano SEF redova: 200". |
| `sales/sef/sef.service.ts:229-251` | 🟡 | `buildInvoicePdf` padne (npr. `assertExportWithoutVat`, prazan IBAN izdavaoca, nepoznat obrazac) → e-faktura ode **bez PDF priloga**, uz `logger.warn`. | `enqueue` vrati 201 sa `warning: null` — polje `warning` nosi isključivo „javni sektor bez broja narudžbenice" (`:302-308`). Korisnik ne dobija nikakav znak da prilog fali. |
| `sales/sef/ubl-builder.service.ts:463, 561-569` | 🟡 | Domaći račun sa valutom `EUR` (dostižno kroz nalaz iz §1) ide na SEF sa `DocumentCurrencyCode=EUR` i porezom iskazanim samo u EUR — `cbc:TaxCurrencyCode` i drugi `cac:TaxTotal` u RSD se ne emituju. | Izvoz je izričito zabranjen za SEF (`:132-136`), pa se pretpostavlja da su svi SEF dokumenti u RSD; ništa to ne proverava. |

**Pokrivenost `sef-neuspeh-nije-uspeh.spec.ts` (520 linija) + `sef.service.spec.ts`.**
Pokriveno i dobro: N1 kapija po registru vrsta (uklj. fail-closed i ulazni smer), N2 `cancel` baca uz
`CANCEL_PENDING` (timeout / DRY-RUN / ponovni pokušaj / polling ne gazi stanje / lokalno otkazivanje
bez `sefInvoiceId`), N3 brana za `DELIVERED` uz proveru da ruta za storniranje ne postoji, CAS pri
`send` u sve tri varijante (uklj. „SEF `ok` bez identifikatora"), `PrepaidAmount` ↔ `BillingReference`.
**Nepokriveno:** dupli `enqueue` (najteži nalaz ovog poglavlja), `enqueue` nad `CANCELLED` fakturom,
tok `REJECTED`, brisanje `errorMessage` pri nepoznatom SEF statusu, granica od 200 redova pri stornu,
tiho izostavljanje PDF priloga.

---

## 3. Numeracija i prepis dokumenata

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `sales/fakturisanje.service.ts:1560-1580` | 🟠 | `nextJournalNumber` računa MAX u JS-u nad **svim** nalozima `(firma, vrsta, godina)` — `findMany` bez `take`. Pri 30.000 IF naloga godišnje svako knjiženje računa učitava 30.000 redova **unutar transakcije, pod advisory bravom** koja serijalizuje sva knjiženja prodaje. Isti obrazac i u `gl/posting/posting.service.ts:nextJournalNumber`. | Na današnjoj produkciji (0 faktura, izmereno u komentarima modula) je instant. Degradacija je linearna i pojavi se tek pošto sistem počne da se koristi. |
| `nabavka/purchase-numbering.service.ts:41-52` | 🟠 | Isto (MAX u JS-u nad svim redovima godine) **plus** `where: { [field]: { endsWith: "/2026" } }` = `LIKE '%/2026'`, koji ne može da koristi indeks → sekvencijalno skeniranje cele tabele pri **svakom** kreiranju zahteva i narudžbenice, unutar transakcije i pod bravom. | Isto: mala tabela danas. `endsWith` deluje kao običan filter. |
| `nabavka/purchase-numbering.service.ts:34, 57-76` | 🟡 | Nabavka **nema tabelu sekvenci** (prodaja ima `document_number_sequences`) — broj je uvek `MAX+1` nad postojećim redovima. Arhiviranje/čišćenje starih godina ili brisanje poslednjeg reda u bazi vraća broj u opticaj: sledeća narudžbenica dobija broj koji je već odštampan i poslat dobavljaču. Unique indeks (`uq_purchase_orders_number`) to ne hvata jer starog reda više nema. | Prodaja je ovo rešila sekvencom i to je opširno obrazloženo (`numbering.service.ts:8-13`); u nabavci se pretpostavlja da važi isto pravilo. |
| `nabavka/purchase-numbering.service.ts:34` | 🟡 | `businessYear()` se zove **bez argumenta** = tekuća godina, a broj se vezuje za dokument koji može nositi drugi datum. | Broj i datum se raziđu samo oko Nove godine. |
| `sales/fakturisanje.service.ts:392` i `sales/advance-invoice.service.ts:294, 419` | 🟡 | Godina za numeraciju se izvodi sa `getFullYear()` (kontejner radi u UTC), a ne sa `businessYear()`. Predračun kreiran 01.01.2026. u 00:30 po Beogradu (= 31.12.2025. 23:30 UTC) dobija broj iz **prošlogodišnjeg** niza (`PROF-265/25`) uz `documentDate` u 2026. Posle toga `SalesService.updateHeader` (`sales.service.ts:191-201`) odbija svaku izmenu datuma na tom dokumentu, jer broj i datum pripadaju različitim godinama. | `common/business-date.ts` je napisan baš protiv ovoga („dokument knjižen 1.1. u 00:30 po Beogradu bi otišao u PROŠLOGODIŠNJU sekvencu"), pa se pretpostavlja da ga svi pisci broja koriste. `postInvoice` ga koristi (`:635`) — `createProforma` i oba puta AVR-a ne. |
| `sales/fakturisanje.service.ts:1411-1431` (`assertCreditLimit`) | 🟠 | **Potvrda poznatog nalaza** + tri dopune. Predikat `je.status IN ('POSTED','LOCKED')` i predikat otvorene stavke (`reconciled_at IS NULL`, `tracks_open_items`, `side='receivable'`, `partner_scope='customer'`) jesu prepis iz `saldakonti/open-items.service.ts` i voze branu limita — **potvrđeno**. Dodatno: (a) upit **ne filtrira po `companyId`**, pa se dug meri preko svih firmi dok se limit vodi na kupcu; (b) provera je van transakcije i **pre** CAS-claim-a (`:633` vs `:664`), pa dva paralelna knjiženja oba prođu limit; (c) odbijeni avans se ne oduzima — kupac koji je avansom pokrio ceo račun ipak može oboriti limit i dobiti 422. | Guard ima `force` izlaz, pa se svako lažno pozitivno rešava klikom „Proknjiži uprkos limitu" i ne prijavljuje se kao bug. Lažno negativno (dug iz druge firme ne postoji, paralelno knjiženje) se ne vidi uopšte. |

---

## 4. Poslovna rupa (šta prodaja/nabavka mora, a nema)

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `nabavka/nabavka.service.ts:532-633` (guard `:544-545`, `:554-561`, status `:623-626`) | 🔴 | **Delimična isporuka ne postoji.** Naručeno 100 kom, stiglo 60. Primiš 60 → jedan robni ulaz, PO ide na `RECEIVED`. Kad stigne preostalih 40, prijem se **ne može evidentirati nikako**: drugi prijem pada na 409 „već primljena/zatvorena", a i da ne padne, `uq_stock_documents_po` dozvoljava samo jedan ulaz po narudžbenici. Ostatak nikad ne uđe na zalihe, a 3-way match trajno prikazuje manjak koji se ne može zatvoriti. | Prvi prijem prođe savršeno. Problem se pojavi tek nedelju dana kasnije, kad stigne ostatak — i tada izgleda kao „greška u unosu", a ne kao rupa u modelu. Sama poruka („već primljena") potvrđuje tu pogrešnu interpretaciju. |
| `nabavka/nabavka.service.ts:566-577` + `nabavka.controller.ts:265-274` | 🔴 | Narudžbenica sa 5 stavki, stigla je samo stavka 3. Pošalješ `lines: [{itemId: 3, receivedQuantity: 10}]`. Ostale **4 stavke tiho dobijaju `receivedQuantity = orderedQuantity`** (default u `:571`), robni ulaz se pravi za svih 5 (`:581-608`), zaliha se poveća za robu koja nije stigla, kalkulacija i GL knjiže punu vrednost, a 3-way match kaže „sve primljeno". Prazno telo (`body.lines ?? []`, kontroler `:273`) prima **celu** narudžbenicu jednim pozivom. | Nema nijedne poruke — default „primljeno = naručeno" je dokumentovan kao BigBit paritet, pa se ponaša kao namera i kad korisnik pošalje SAMO jednu stavku. Odstupanje isplivava tek u inventuri. |
| `sales/sales.service.ts:440-447` vs. `sales/print/invoice-pdf.service.ts:372-386` i `sales/numbering.service.ts:227-242` | 🔴 | **Knjižno odobrenje/zaduženje ne postoji kao dokument.** Kod korisniku poručuje „ispravka ide protivdokumentom: knjižno odobrenje (umanjenje) ili knjižno zaduženje (uvećanje)", a u sistemu postoji samo **štampa**: `buildCreditNotePdf(id)` odštampa postojeću fakturu pod naslovom KO, sa **pozitivnim** iznosima, bez novog broja, bez svog GL naloga i bez serije u `DOCUMENT_SERIES` (nema ni `KO` ni `KZ`). Kupac reklamira 2 od 10 komada na proknjiženom računu → jedini put je STORNO celog računa pa nov račun; a ako je e-faktura na SEF-u `DELIVERED`, storno pada na `sef.service.ts:656-671` i ispravan put **ne postoji uopšte**. | Poruka o grešci zvuči kao uputstvo, pa se pretpostavlja da ruta postoji. `?variant=credit-note` postoji na ruti štampe i vraća uredan PDF, što tu pretpostavku potvrđuje. |
| `nabavka/nabavka.service.ts:328-401` | 🟠 | **Prihvaćena ponuda ne pamti cenu.** `offeredPrice` iz tela se koristi samo da napuni `createOrderDraft` u ODGOVORU (`:389-397`); u bazu se ne upisuje nigde (`SupplierRfqItem` nema kolonu cene). Nabavka prihvati ponudu, zatvori ekran, sutra pravi narudžbenicu → cene više nema, mora ponovo iz mejla. Poređenje ponuda više dobavljača — glavni razlog zašto RFQ uopšte postoji — nije moguće ni u jednom ekranu ni izveštaju. | Odgovor rute sadrži cene, pa ekran koji odmah nastavi u narudžbenicu radi ispravno. Gubitak se vidi tek pri povratku na upit. Komentar to i opravdava („cena tek u narudžbenici, BigBit pravilo") — ali BigBit pravilo govori gde se cena UNOSI, ne da se ponuda ne čuva. |
| `nabavka/nabavka.service.ts:355-364` | 🟠 | `acceptQuote` označava `isAccepted: true` **svim** stavkama upita, i onima koje dobavljač uopšte nije ponudio. Zatim `createOrderDraft` (`:389`) predloži narudžbenicu za sve njih, sa `unitPrice: null` za neponuđene. | Narudžbenica sa cenom `null` prolazi validaciju (`create-purchase-order.dto.ts:44-48` traži cenu samo ako je poslata), pa se naruči roba bez ugovorene cene — a 3-way match je onda ne poredi (`three-way-match.service.ts:845-851` traži `orderedPrice > 0`). |
| — (grep po `sales/**` i `nabavka/**`) | 🟠 | **Povrat robe ne postoji kao tok** ni ka dobavljaču ni od kupca. `REV` je revers (zaduženje opremom) i izričito je izbačen sa SEF-a. Reklamacija se rešava stornom celog dokumenta. | Postoji `REV` u registru serija i u `TARGET_TYPES` prepisa, pa deluje kao da povrat postoji. |
| `sales/pricing.service.ts:476-498` | 🟠 | **Cenovnik bez važenja i bez redosleda.** `resolveBasePrice` radi `findFirst` po `(itemId, documentTypeId)` **bez `orderBy`** i bez `validFrom/validTo`. Artikal sa dva reda za isti cenovnik (stari + nov) daje cenu koju baza slučajno vrati prvu. | Rabat u istom fajlu (`:554-562`) filtrira po važenju, pa se pretpostavlja da i cenovnik to radi. Većina artikala danas ima jedan red; kad se cenovnik ažurira **dodavanjem** reda umesto izmenom, deo dokumenata tiho dobija staru cenu. |
| `sales/pricing.service.ts:545-579` | 🟡 | Dva preklapajuća važeća rabata za istu grupu → `find` uzima prvi po `validFrom: desc`, bez ijedne poruke da sukob postoji. Isto i za dva flat rabata. | Ishod je uvek „neki" rabat i uvek deluje namerno. |
| `nabavka/nabavka.service.ts:592-593` vs. `nabavka.controller.ts:265-274` | 🟡 | Prijem pada na tvrde podrazumevane vrednosti `warehouseId ?? 1` i `documentTypeCode ?? "UFROB"`, a kontroler `opts` **uopšte ne prosleđuje**. Firma sa više magacina: svaki prijem po narudžbenici ulazi u magacin 1, bez pitanja i bez poruke. | Zaliha se poveća, kalkulacija prođe, GL knjiži — sve „radi". Otkriva se u inventuri drugog magacina. |
| `sales/print/invoice-mail.service.ts:44-92` | 🟡 | Slanje računa kupcu **nema nijednu proveru statusa**. Klik na „Pošalji na mail" nad nacrtom iz prepisa → kupac dobija PDF sa brojem `DRAFT-44` i naslovom „Račun DRAFT-44". Nad storniranim računom mejl ide isto (PDF nosi značku „STORNIRANO", ali telo mejla kaže „U prilogu Vam dostavljamo račun"). | Servis čita samo `documentNumber/documentType/customerId/isExport` (`:45-52`) — status ni ne učitava, pa nema šta ni da proveri. Ruta vrati `{ sent: true }`. |

---

## 5. 500 umesto jasne poruke · performansa pri rastu

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `nabavka/nabavka.controller.ts:129-142` | 🟠 | `POST /nabavka/requests/:id/send-rfq` prima `supplierId` i `supplierEmail` iz tela **bez ijedne validacije** (nema `validate*` poziva). Telo bez `supplierId` → `tx.supplierRfq.create({ supplierId: undefined })` → Prisma validation error → **goli 500 „Internal server error"**. Adresa se ne proverava uopšte: upit sa predmetom i količinama ode na proizvoljan mejl, a ruta vrati 200 sa `emailSent: true`. | Sve ostale rute modula imaju `validate*` DTO (`createRequest`, `createOrder`, `acceptQuote`), pa se pretpostavlja da i ova ima. |
| `nabavka/nabavka.service.ts:637-658` | 🟠 | `listRequests` ima `take: query.take ?? 50` **bez gornje granice**, uz `include: { items: true }`. `GET /nabavka/requests?take=1000000` učita sve zahteve sa svim stavkama u memoriju procesa. | `listRfqs` (`:272`) i `listOrders` (`:482`) u istom fajlu imaju `Math.min(…, 200)` — ova jedina nema, pa izgleda kao da je pokrivena istim pravilom. |
| `nabavka/three-way-match.service.ts:284-316` | 🟠 | **Potvrda i ispravka poznatog nalaza.** Tačno je da `meta.total` laže iznad 1000 narudžbenica i da kod to loguje (`:291-295`). **Nije tačno** da se `findingCount > 0` filtrira posle `LIMIT`-a — od te ispravke se skenira do `MATCH_SCAN_LIMIT`, filtrira, pa tek onda paginira, pa je `total` tačan do granice. Cena je nova: **svaki** poziv sa `onlyWithFindings=true` (i druga strana, i `GET /match-summary/pdf`) ponovo skenira do 1000 narudžbenica u 5 batch-eva × 4 upita, učitava im sve stavke, sve robne ulaze sa stavkama i sve KUF redove, i računa sve nalaze — da bi vratio stranu od 50 redova. | Odgovor je ispravan i brz na današnjem obimu; nema ni upozorenja ni metrike koja bi pokazala koliko je posla obavljeno po strani. |
| `nabavka/three-way-match.service.ts:491-507` | 🟠 | Robni ulazi se čitaju **bez filtera po statusu** i bez `deletedAt` na nivou DOKUMENTA (filter postoji samo na stavkama, `:503`). Storniran/otkazan robni ulaz i dalje ulazi u „fakturisano": 3-way match tvrdi da je narudžbenica fakturisana dokumentom koji više ne važi, i pravi `PRICE_VARIANCE` po ceni sa poništenog ulaza. | Status se čita (`:497`), ali se koristi isključivo za `invoiceBooked` (`:606-609`) — deluje kao da je filtriranje pokriveno. |
| `nabavka/nabavka.controller.ts:211-214` + `nabavka.service.ts:497-502` | 🟠 | `GET /nabavka/orders/999999` → HTTP **200 sa telom `null`**. Frontend puca na `data.items` ili prikaže prazan ekran bez poruke. | Sve ostale rute modula imaju `…OrThrow` / eksplicitan 404 (`getRequestDetail`, `getRfq`, `getOrderOrThrow`) — ova jedna ne. |
| `sales/fakturisanje.service.ts:440-463` | 🟡 | `listInvoices` sortira samo po `documentDate: "desc"`, bez sekundarnog ključa. Kad više dokumenata deli isti datum (redovna pojava — svi računi jednog dana), redosled među njima nije determinističan, pa paginacija **preskače i duplira** redove između strana. | Vidi se samo kao „nedostaje mi jedna faktura u listi", što se pripiše filteru. Isto i `nabavka.listOrders`/`listRfqs` (`createdAt: desc`), tamo je rizik manji zbog finije granulacije. |
| `sales/advance-invoice.service.ts:857-882` | 🟡 | P2002 se prevodi u čitljiv 409 **samo** za `invoiceAdvanceApplication.create`. Svaki drugi P2002 na istom putu — npr. sudar `documentNumber` u `createAdvanceInvoice` (`:295-300`, dostižno kad je sekvenca zaostala iza zatečenih brojeva, v. „S9 seed brojača") — izlazi kao goli 500. | Poruka „Internal server error" na izdavanju avansa izgleda kao pad servera, a ne kao stanje podataka koje korisnik može da popravi. |
| `sales/sales.service.ts:385-397` | 🟡 | `removeItem` prenumeriše preostale stavke `update`-om **po redu u petlji**. Brisanje prve stavke na dokumentu od 200 stavki daje 200 pojedinačnih UPDATE-a unutar transakcije, plus `recalcTotals`. | Tipičan dokument ima 5–20 stavki; problem se pojavi tek na uvezenim/velikim dokumentima. |
| `nabavka/three-way-match.service.ts:416-425` | 🟡 | `warningsForPayment` tiho odseca na `MATCH_BATCH_LIMIT` (200) narudžbenica — priprema plaćanja za komitenta sa više od 200 narudžbenica u 12 meseci dobija **nepotpun** spisak upozorenja. | Loguje se WARN, ali pozivalac (ekran pripreme plaćanja) dobija uredan niz upozorenja bez ijedne oznake da je odsečen. Upozorenja su informativna, pa se izostanak ne primeti. |

---

## TOP 10 za ovaj domen

| # | nalaz | fajl:linija | ozb. | trud |
|---|---|---|---|---|
| 1 | **Devizna faktura se knjiži u GK nominalno, bez preračuna u RSD** — `fxDebit/fxCredit/fxCurrency` se nikad ne pune, `exchangeRate` se ne čita. Prihod i potraživanje izvoznog računa su ~117× manji, a FX revalorizacija tu stavku odbaci kao `NO_FX_PAIR`. | `sales/fakturisanje.service.ts:1521-1548`; `sales/advance-invoice.service.ts:577-620, 890-933` | 🔴 | **L** |
| 2 | **Delimična isporuka ne postoji** — jedan robni ulaz po narudžbenici + status `RECEIVED` posle prvog prijema; ostatak isporuke se ne može evidentirati nikako. | `nabavka/nabavka.service.ts:532-633` | 🔴 | **L** |
| 3 | **Prijem upisuje `receivedQuantity` i stavkama kojih nema u zahtevu** (default = naručeno), pa jedan primljen red primi celu narudžbenicu na zalihe i u GK. | `nabavka/nabavka.service.ts:566-577`; `nabavka.controller.ts:273` | 🔴 | **S** |
| 4 | **`enqueue` nema anti-duplo guard** — dva klika = dve e-fakture na isti broj na SEF-u; dokumentovana „idempotencija po `requestId`" po konstrukciji ne može da radi. | `sales/sef/sef.service.ts:125-375`; `sef/sef.controller.ts:137-148` | 🔴 | **S** |
| 5 | **Knjižno odobrenje/zaduženje postoji samo kao naslov na štampi** — nema vrste, broja, serije ni GL naloga; kod ga korisniku preporučuje kao jedini ispravan put, a za `DELIVERED` e-fakturu drugog puta uopšte nema. | `sales/sales.service.ts:440-447`; `print/invoice-pdf.service.ts:372-386`; `numbering.service.ts:227-242` | 🔴 | **L** |
| 6 | **Naplaćen avans se ne veže za konačni račun** — ništa pri knjiženju ne proverava postoji li naplaćen AVR po istom predračunu; kupac se zaduži dvaput. | `sales/fakturisanje.service.ts:600-867` | 🔴 | **M** |
| 7 | **Promena kupca / valute na nacrtu ne dira nijedan iznos** — novi kupac dobija tuđi rabat; RSD iznos postane EUR iznos. | `sales/sales.service.ts:203-211, 745-748`; `dto/update-invoice.dto.ts:218-225` | 🔴 | **M** |
| 8 | **`assertCreditLimit`: prepisan predikat otvorene stavke + bez `companyId` + van transakcije + ne uzima avans u obzir** (potvrđen poznat nalaz, sa tri dopune). | `sales/fakturisanje.service.ts:1396-1450` | 🟠 | **M** |
| 9 | **Prihvaćena ponuda ne pamti cenu, a označava sve stavke prihvaćenim** — poređenje ponuda dobavljača nije moguće; narudžbenica se pravi bez ugovorene cene. | `nabavka/nabavka.service.ts:328-401` | 🟠 | **M** |
| 10 | **SEF: `REJECTED` ne pokreće ništa, `enqueue` prolazi nad storniranom fakturom, nepoznat status briše `errorMessage`, storno gleda samo 200 redova** (četiri rupe u istom životnom ciklusu outbox-a). | `sales/sef/sef.service.ts:137-141, 553-609`; `fakturisanje.service.ts:1356-1362` | 🟠 | **M** |

**Trud:** S = jedan fajl, do pola dana · M = nekoliko fajlova + test, 1–2 dana · L = izmena modela/šeme
i migracija, uz odluku vlasnika ili knjigovođe.

### Tri koja traže tuđu odluku pre koda
- **#1 (valuta u GK)** — kurs koji se koristi za knjiženje (`exchangeRate` vs `accountingExchangeRate`)
  i tretman kursne razlike pri naplati mora da potvrdi knjigovođa.
- **#5 (knjižno odobrenje)** — nova vrsta dokumenta, njena serija, njen konto i da li ide na SEF
  (`InvoiceTypeCode 381`) su poslovna odluka, ne tehnička.
- **#2 (delimična isporuka)** — da li narudžbenica sme da ima više prijema traži izmenu
  `uq_stock_documents_po`, dakle dogovor sa vlasnikom modula `robno` (koji se trenutno refaktoriše).
