# DUBINSKI REVIEW — domen NOVAC (saldakonti · placanja · izvodi · kamata)

Baza: `C:\Users\nenad.jarakovic\wt\robno-quality\backend\src`
Datum: 04.08.2026. · Metod: čitanje koda (bez pokretanja); svaki nalaz ima scenario.
Legenda ozbiljnosti: **K** = kritično (tiho pogrešan novac / novac napolje), **V** = visoko,
**S** = srednje, **N** = nisko.

---

## 0. Potvrda „već poznatog"

| tvrdnja | nalaz |
|---|---|
| `je.status IN ('POSTED','LOCKED')` prepisan 20× / 14 fajlova; u ovom domenu 9 mesta | **POTVRĐENO** — `open-items.service.ts:217/274/354`, `partner-card.service.ts:160/187`, `bank-statement.service.ts:283/905`, `kamata.service.ts:181`, `payment-preparation.service.ts:141`. **DOPUNA (bitnija od duplikata):** postoji i 10. kopija u drugom obliku (`reconciliation.service.ts:299`, JS provera `r.status !== "POSTED" && !== "LOCKED"`) i — ključno — **jedno mesto gde predikat NEDOSTAJE**: `compensation.service.ts:346-355` čita članove grupe bez ijednog uslova nad `journal_entries.status`. To je dokaz da duplikat više nije samo DRY-problem nego je već proizveo divergenciju (v. nalaz 1.11). |
| Trojka „otvorena stavka" 6× u 3 fajla, isti predikat pokreće IOS i kreditni limit | **POTVRĐENO, ali je stvarni problem drugi**: trojka je *jedina* konzistentna stvar. Nekonzistentan je **izbor konta iznad nje** — pet čitalaca bira konta na pet načina (`side` / `side+partner_scope` / `partner_scope` / ništa). To i pravi razilaženje IOS-a, aging-a, kamate i limita (nalazi 1.1–1.3). |
| `bank-statement.service.ts:176` — 3 upita po redu izvoda u petlji | **POTVRĐENO i gore**: do **5** upita po redu (`matchCustomer` 1–2 + `matchOpenItem` 1–2 + `update` 1), i petlja **nije u transakciji** — prekid ostavlja pola izvoda upareno. |
| `saldakonto_accounts` prazna na produkciji | **POTVRĐENO da je posledica tiha i totalna** (nalaz 1.17). Seed od 9 konta postoji kao migracija `20260726100000_seed_saldakonto_i_seme_kontiranja`, ali je u `git status` još **untracked (`??`)** — dakle nije ni komitovana ni primenjena. Dok se ne primeni: IOS/kartica/aging/opomene/priprema plaćanja/revalorizacija vraćaju prazno **bez ijedne poruke**, a `assertCreditLimit` vidi saldo 0 → kreditni limit propušta svaku fakturu. |

---

## 1. Tiho pogrešan iznos duga / potraživanja

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `saldakonti/collection-dashboard.service.ts:75-97` + `saldakonti/open-items.service.ts:336` | **K** | Na jednom ekranu „Naplata" KPI `totalReceivable` se računa iz `side='receivable'` (= 2040, 2050, **+1520/1521/1530 = avansi koje smo MI platili dobavljačima**), a `aging.total`/`topDebtors` iz `agingByPartner` bez konta, koji filtrira `partner_scope='customer'` (= 2040, 2050, **+4300/4302 = avansi koje su nama platili kupci, potražni**). Sa 500.000 avansa dobavljaču i 300.000 avansa od kupca: KPI kaže potraživanja 800.000, tabela aging ispod njega kaže 0. Ista reč, dva broja, jedan ekran. | Oba broja su „razumna", nijedan nije nula, razlika izgleda kao zaokruživanje dok neko ne sabere kolonu. Nema testa koji poredi `Σ aging.total` sa `totalReceivable`. |
| `saldakonti/dunning.service.ts:104,400` + `dunning-pdf.service.ts:115` | **K** | Opomena bira po `it.side !== "receivable"` → **bez** `partner_scope`. Otvorena stavka `1520 / komitent 77 / AV-3/26 / 500.000 / dospeće 01.03.` je `receivable` sa dugovnim saldom → dobavljač kome smo PLATILI avans dobija mejl „OPOMENA PRED UTUŽENJE" na 500.000 i PDF sa tom stavkom. | `loadProfiles` čita iz iste `customers` tabele, pa ime i mejl postoje — mejl izgleda ispravno. Primeti se tek kad dobavljač odgovori. `assertCreditLimit` (`fakturisanje.service.ts:1428`) je isti bag popravio filterom `partner_scope='customer'`; dunning nije. |
| `kamata/kamata.service.ts:161-164` | **V** | Isto kao gore, u kamatnom listu: `side:'receivable'` hvata 5 konta. Dati avans 500.000 / 154 dana / 9,50 % → 20.041,10 zatezne kamate dobavljaču. **Potvrđeno** — dokumentovano kao K-1 u kodu i `PREOSTALE_FAZE.md`, i dalje otvoreno. | Kamatni list je DRAFT i nema štampu (nalaz 3.1), pa ga niko ne pregleda red-po-red. |
| `saldakonti/dunning.service.ts:401` | **V** | `aggregateOverdue` preskače stavke sa potražnim saldom (`if (!it.balance.greaterThan(0)) continue`), ali ih **ne oduzima**. Kupac koji je preplatio fakturu A za 200.000 i kasni 20 dana sa fakturom B od 90.000 dobija opomenu na 90.000, iako mu mi dugujemo 110.000. | Opomena je matematički tačna „po dokumentu"; niko ne gleda neto poziciju partnera pre slanja, a saldo na kartici je −110.000. |
| `kamata/kamata.service.ts:205,226-238` | **V** | Netiranje briše vremensku dimenziju: faktura 1.000.000 dospela 01.01., kupac plati 900.000 dana 05.01. Grupa daje `principal = 100.000`, `dueDate = 01.01.`, a `days` se meri od 01.01. do preseka. Kamata na 1.000.000 za prva 4 dana **potpuno ispada**. Firma sistematski potcenjuje zateznu kamatu, uvek u svoju štetu. | Rezultat nije nula i nije apsurdan — samo je manji. Ispravan obračun (po periodima) nema se sa čim uporediti jer BigBit list niko ne unakrsno proverava. |
| `kamata/kamata.service.ts:240` vs `:318-330` | **S** | Linije se zaokružuju na 4 decimale, `totalInterest` je zbir 4-decimalnih vrednosti, a i linije i total se **prikazuju** sa `toFixed(2)`. Na 12 stavki Σ prikazanih linija ume da odstupi od prikazanog totala za par para. Kamatni list je dokument koji ide kupcu. | Razlika je 0,01–0,05 din; kupac je ne prijavljuje, a knjigovođa je pripiše zaokruživanju. |
| `kamata/kamata.service.ts:234-236` | **S** | Konformni metod ide kroz `Math.pow(...)` i `rateFraction.toNumber()` → **Float na putanji novca** (BACKEND_RULES §2), pa `new D(factor)` unosi binarni šum u Decimal. | Odstupanje je u 12. decimali; vidi se tek kao „zašto se ista kamata dva puta ne poklapa". |
| `izvodi/bank-statement.service.ts:669-712` | **K** | `LedgerLineDraft` nema `fxDebit/fxCredit/fxCurrency`. Uplata po EUR fakturi (devizni izvod) knjiži **samo RSD**. Faktura 1.000 EUR / 117.000 RSD, uplata 117.300 RSD: grupa ostaje sa deviznim saldom **1.000 EUR** i dinarskim −300 → 31.12. revalorizacija vidi „otvorenu deviznu stavku od 1.000 EUR" na već plaćenoj fakturi. | `fx-revaluation.checkFxPair` je uhvati kao `RATE_MISMATCH` i **isključi iz obračuna** — dakle ne knjiži pogrešno, ali tiho izbaci iz bilansnog preseka, pa je knjigovođa mora raditi ručno i ne zna zašto. Bez `force` niko ne vidi iznos. |
| `izvodi/bank-statement.service.ts:642-643,681` | **K** | Konto komitentske strane je **hardkodovan po smeru**: priliv → `2040`, odliv → `4350`. Dobavljač vrati preplatu od 50.000 (priliv) → knjiži se `2040 / dobavljač / potražuje 50.000`. Nastaje fantomski „kupčev" kredit od −50.000 koji obara `totalReceivable` na tabli naplate, dok stvarno potraživanje prema dobavljaču (1520/4350) ostaje otvoreno. | Nalog balansira, izvod je POSTED, kontrola salda banke se slaže. Registar `saldakonto_accounts` postoji baš za ovo, ali ga ova ruta ne čita (TODO u kodu). |
| `saldakonti/compensation.service.ts:346-355` | **K** | Upit koji meri „otvoreni saldo grupe" pre prebijanja **nema** `journal_entries.status` uslov (jedino mesto u domenu gde nedostaje) i, kad je `documentNumber` NULL, `where: { documentNumber: null }` hvata **sve** stavke tog partnera i konta bez broja dokumenta. Nacrtna (DRAFT) faktura od 400.000 na istom dokumentu naduva `openBalanceAbs` → pun offset se klasifikuje kao delimičan → **ništa se ne zatvara**, a protivstavka je već proknjižena. Obrnuto: kod grupe bez broja dokumenta pun offset od 1 din zatvori (`reconciledAt`) sve stavke partnera bez broja. | Kompenzacija se prikaže kao POSTED sa tačnim `totalAmount`; posledica je vidljiva tek u IOS-u sledećeg meseca. |
| `saldakonti/dto/saldakonti.dto.ts:88-120` + `compensation.service.ts:300-317` | **K** | `CompensationLineInput.ledgerEntryId` se ne proverava **ni po čemu**: ni da pripada `dto.partnerId`, ni da je otvorena, ni da je na kontu deklarisane `side`, ni da `amount ≤ otvoreni saldo`. `postCompensation` čita konto/komitenta/broj **iz same stavke** — greška u jednoj cifri ID-a prebija dug **trećeg partnera** i zatvara njegovu fakturu. | Bilans DTO-a (`Σ receivable == Σ payable`) prolazi, PDF izjave se odštampa, broj kompenzacije je tačan. Partner čija je faktura zatvorena to vidi tek na IOS-u. |
| `saldakonti/compensation.service.ts:79-83,391-393` | **S** | `sumAbs` uzima `balance.abs()` po strani. Kupac koji je preplatio (receivable grupa sa saldom −80.000) ulazi u `totalReceivable` kao **+80.000** → predlog kompenzacije nudi 80.000 više nego što postoji za prebijanje. | Predlog je samo predlog; ako ga korisnik prihvati kako jeste, greška se materijalizuje kroz nalaz iznad (nema provere `amount ≤ saldo`). |
| `saldakonti/saldakonti.controller.ts:512-516` | **K** | `parseOptionalDate` na nevalidan datum **tiho vrati `undefined`**. `GET /saldakonti/ios-pdf?partnerId=77&asOf=31.12.2026` (srpski format!) → `new Date("31.12.2026")` = Invalid → presek postaje **danas**. IOS obrazac za godišnje usaglašavanje nosi današnje stanje, a u zaglavlju piše današnji datum, pa ništa ne vrišti. Isto važi za `aging`, `open-items`, `collection-dashboard`, `dunning/candidates`, `partner-card`. | Nema greške, nema upozorenja, PDF se otvori. Razlika između 31.12. i npr. 15.01. je nekoliko faktura — izgleda kao normalan promet. |
| `saldakonti/saldakonti.controller.ts:506-510` | **S** | `parseOptionalInt` na nevalidan/0/negativan ulaz vrati `undefined`. `GET /saldakonti/open-items?partnerId=abc` (ili `partnerId=0`) → filter otpada → vrate se **otvorene stavke SVIH komitenata** kao da su jednog. | Odgovor je 200 sa podacima; `meta.count` je veliki ali korisnik misli da partner ima mnogo stavki. |
| `placanja/payment-preparation.service.ts:176,189,211` | **V** | Saldo grupe se računa iz `credit − debit`, tj. iz **dinarskih** kolona, ali se `currency` uzima iz `le.currency` (valuta dokumenta). Devizna obaveza 1.000 EUR / 117.000 RSD daje `openAmount = 117000.0000`, `currency = "EUR"` → predlog i nalog za plaćanje glase **117.000 EUR**. | Broj na ekranu je tačan; pogrešna je samo troslovna oznaka pored njega. Iznos ide u banku kroz `payment-export` koji valutu uopšte ne gleda (nalaz 3.4). |
| `saldakonti/partner-card.service.ts:135-245,490-509` | **S** | Bez `accountCode` kartica sabira **sve** saldakonto konte partnera u jedan running saldo. Za kooperanta koji je i kupac (2040: +900.000) i dobavljač (4350: −700.000) zatvaranje kartice je 200.000 i PDF piše „komitent duguje nama 200.000", dok IOS istog partnera pokazuje dve odvojene stavke, a aging 900.000. | Sva tri dokumenta su „tačna" po svojoj definiciji; niko ne piše koja je definicija na papiru. |
| `saldakonti/open-items.service.ts:193-232` (svi čitaoci) | **K** | `saldakonto_accounts` je **INNER JOIN**. Kad je registar prazan (produkcija, potvrđeno), `listOpenItems` vrati `[]` → IOS je NIOS („saldo 0, nema otvorenih stavki") za dužnika od 12 miliona, aging je prazan, tabla naplate pokazuje 0, `dunning/candidates` je prazan, `kamata.compute` baca **400 „Nema otvorenih dospelih stavki"**, `selectDue` vrati `[]` (`accountCodes.length === 0`), a `assertCreditLimit` izračuna saldo 0 pa **propusti svaku fakturu bez obzira na limit**. | Nijedan od tih odgovora nije greška — svi su „uredno prazni". Sistem izgleda kao da firma nema ni jednog dužnika. Migracija koja to popravlja postoji ali je **untracked**. |

---

## 2. Uparivanje izvoda

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `izvodi/bank-statement.service.ts:329-336` (+ `:280-284`) | **K** | Fallback po iznosu: `baseWhere` je samo `{analyticalCode, reconciledAt:null, status POSTED/LOCKED}` — **bez** JOIN-a na saldakonto registar, **bez** smera, **bez** `orderBy`. Kupac plati fakturu 657/25 (120.000) u januaru; u martu isti kupac plati drugu fakturu od tačno 120.000 sa nečitkim PNB-om → `findFirst` nađe **januarsku fakturu** (i dalje `reconciledAt = null`, v. nalaz ispod) i knjiženje upiše `657/25`. Rezultat: martovska faktura ostaje 100 % otvorena (kamata + opomena), a na 657/25 visi lažna preplata od −120.000. | Uparivanje je „uspelo", stavka je MATCHED, izvod se proknjiži bez ijednog upozorenja. `findFirst` bez `orderBy` znači da isti podaci mogu dati različit rezultat u dva pokretanja. |
| `izvodi/bank-statement.service.ts:764-766` | **K** | `postStatement` **nikad ne poziva reconcile** (TODO u kodu). `reconciled_at` ostaje NULL i na fakturi i na uplati zauvek; zatvaranje se oslanja isključivo na to što se grupa netuje na 0 pa ispadne iz `HAVING balance <> 0`. Posledica: plaćene fakture su i dalje „otvorene" za `matchOpenItem` (gornji nalaz), za `payment-preparation.selectDue`, i za `MaxSaldo`. | Izveštaji izgledaju tačno (netiranje radi), pa se nedostatak `reconciled_at` vidi samo kroz drugorazredne posledice. |
| `izvodi/bank-statement.service.ts:616` vs `:658,759` | **V** | `statement.lines` se učitava **pre** `$transaction`, a `updateMany({ where: { statementId } })` unutar transakcije markira **sve** linije kao POSTED. Kolega doda stavku od 300.000 (`POST /izvodi/:id/lines`) sekundu pre nego što prvi klikne „Proknjiži": ta stavka **nije** u nalogu GK, ali je označena POSTED, izvod je POSTED, i više se ne može ni izmeniti ni proknjižiti. 300.000 nikad ne uđe u glavnu knjigu. | Kontrola salda banke bi to uhvatila — ali je isključena (nalaz ispod). Stavka na ekranu izgleda proknjižena. |
| `izvodi/bank-statement.service.ts:87-100` + `schema.prisma:3972` | **V** | Ključ idempotencije je `@@unique([bankAccount, statementNumber])` — **bez godine**. Banke resetuju numeraciju izvoda 1. januara. Uvoz izvoda **1 za 2027.** vraća `409 „Izvod 1 za račun ... je već uvezen (id 143)"`. | Poruka zvuči kao ispravna zaštita od duplog uvoza; knjigovođa je uveren da je već uvezao i preskoči izvod. Otkriva se tek kad se ne slaže stanje računa. |
| `izvodi/bank-statement-parser.service.ts:82-90` | **K** | Red se prvo desno-trimuje pa poredi sa `MIN_LINE_LENGTH = 196`. Ako je `DatumDok(189,8)` prazan (a jeste, kod uplata bez datuma dokumenta) i sve iza njega su razmaci, trimovani red je ~188 znakova → **stavka se tiho preskače** uz `logger.debug`. Uplata od 2.400.000 nestane iz uvezenog izvoda. | `debug` nivo se u produkciji ne loguje. `previewParse` vrati manji `count`, ali ga niko ne poredi sa brojem redova u fajlu, a `parse` ne vraća broj preskočenih. |
| `izvodi/bank-statement.service.ts:1056-1071` + `dto/import-statement.dto.ts:26-27` | **V** | Jedina brana za nalaz iznad — „Kontrola prometa i salda" — je **isključena po defaultu**: `openingBalance`/`closingBalance` su opcioni u DTO-u, parser ih ne vadi iz TXT-a, oba ostanu 0 → `available = false` → `ok: true`. Traka je zelena i za izvod kome fali stavka. | „Zeleno" je jače od odsustva podatka; `available` postoji u odgovoru ali FE ga lako prikaže kao „u redu". |
| `izvodi/bank-statement-parser.service.ts:142-150` | **K** | Nepoznat `DugPotInd` → **`CREDIT` (priliv)** uz `warn`. Ako banka promeni layout za jedan znak ili pošalje prazno polje, ceo izvod se uveze kao sami prilivi: odlivi (isplate dobavljačima) postanu naplate od kupaca i zatvore tuđa potraživanja. | Nema greške pri uvozu, izvod ima ispravan broj stavki i ispravne iznose. Uhvatila bi ga kontrola salda — koja je isključena. |
| `izvodi/bank-statement-parser.service.ts:131-139` | **S** | `parseAmount` briše **sve** ne-cifre, uključujući minus. Storno-red sa iznosom `-12345` postaje +123,45 u smeru koji odredi `DugPotInd`. | Iznos je „lep broj", predznak je informacija koje u modelu (`amount Decimal @default(0)` + `direction`) i nema. |
| `izvodi/bank-statement.service.ts:249-255` | **S** | Meki fallback po nazivu: `name contains partnerName, mode:"insensitive"`, `take: 2`, uparuje ako je **tačno jedan** pogodak. Uplatilac „MB" (skraćen naziv u izvodu na 35 znakova) pogodi jedini zapis koji sadrži „MB" — npr. „MB KOMERC DOO" — i uplata sedne tom komitentu. | Rezultat je MATCHED sa punim imenom komitenta koje deluje uverljivo; korisnik ga potvrdi klikom na „Proknjiži". |
| `izvodi/bank-statement.service.ts:625-632` | **V** | Knjiženje se odbija dok postoji ijedna stavka bez `matchedCustomerId`. Realan izvod **uvek** ima „naknada za vođenje računa", porez, doprinose, platu — ti redovi nemaju komitenta. Izvod se **ne može proknjižiti** dok knjigovođa svakoj naknadi ručno ne dodeli lažnog komitenta — čime naknada ode na `4350` kao smanjenje obaveze prema tom „dobavljaču" i tamo ostane otvorena zauvek. | Poruka 422 je jasna („prvo upari"), pa izgleda kao korisnička obaveza, a ne kao rupa u modelu (nedostaje pojam „stavka bez komitenta → konto troška"). |
| `izvodi/bank-statement.service.ts:435-440` + `:501-509` | **S** | `updateLine` briše `matchedLedgerEntryId` **samo** kad se komitent skida na `null`; promena komitenta ostavlja zastarelu vezu. `resolvePostingDocumentNumbers:872-879` to hvata poređenjem `analyticalCode`, ali `linkLineToLedger:506` je već prepisao `referenceNumber` na broj **starog** komitenta — pa korak (2) („sirov PNB je dokazano broj otvorene stavke") može da ga „dokaže" ako novi komitent slučajno ima dokument istog broja. | Dva sloja zaštite izgledaju kao da pokrivaju slučaj; propusna je samo kombinacija link → promena komitenta → knjiženje. |
| `izvodi/bank-statement.service.ts:1103-1118` | **S** | `orderBy: { number: "desc" }` nad **stringom** zero-padovanim na 4. Posle 9999 naloga vrste IZV u godini `"9999" > "10000"` leksikografski → `next` ostaje 10000 zauvek → duplikati broja naloga (nema unique u schemi). Isti obrazac i `parseInt(last.number)` → nenumerički broj daje `"0NaN"`. | Do 9999 radi savršeno; prelazak je jednokratan i tih. |
| `izvodi/bank-statement.service.ts:737-746` | **S** | `companyId: 0` je hardkodovan i za nalog i za `nextJournalNumber`. Ako se ikad uvede druga firma, promet banke joj neće ući u otvorene stavke (`listOpenItems` ima `companyId` filter). | Jednofirmska instalacija to nikad ne pokaže. |
| `izvodi/bank-statement.service.ts:167-205` | **S** | `matchLines` nije u transakciji: prekid na 40. od 80 stavki ostavlja izvod pola-upareno, bez ijedne oznake. Ponovni poziv ide iznova (idempotentan), ali `matchedCount` u odgovoru je onda pogrešan. | Korisnik samo klikne „Upari" ponovo. |

---

## 3. Poslovna rupa (šta knjigovođa mora ručno)

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `kamata/kamata.service.ts:63` + `kamata.controller.ts` (cela) | **V** | `ComputeInterestDto.post?: boolean` („knjiži kamatu u GK") je **deklarisan i nikad pročitan**. Nema rute za knjiženje, nema PDF-a kamatnog lista, nema izdavanja računa za kamatu. Obračun zauvek ostaje `status: "DRAFT"` sa `journalEntryId: null`. Knjigovođa izračuna kamatu u sistemu pa je prekuca u Word i u ručni nalog GK. | Ruta `/kamata/compute` vraća 200 sa kompletnim obračunom — izgleda gotovo. Polje `post` u DTO-u sugeriše da funkcija postoji. |
| `placanja/payment-preparation.service.ts:413-423` | **V** | Dedup po `sourceLedgerEntryId` je **doživotan** — `findFirst` bez ijednog filtera na status/storno. Posledice: (a) **delimično plaćanje dobavljača je nemoguće** — prva rata od 300.000 na fakturu od 1.000.000 zauvek blokira drugu ratu sa `409 „Otvorena stavka je već u nalogu ... dvostruko plaćanje odbijeno"`; (b) nalog kreiran greškom (pogrešan iznos) **trajno** blokira tu obavezu, jer rute za brisanje/storno naloga **nema** (`placanja.controller.ts` ima samo sign/pay/lock/unlock). | Poruka „dvostruko plaćanje odbijeno" zvuči kao ispravna zaštita; korisnik zaključi da je već platio i plati mimo sistema. |
| `placanja/payment-export.service.ts:238-246` | **V** | `supplierName()` **uvek vraća prazan string** (TODO u kodu). U TXT fajl koji ide u banku svaki detaljni slog ima 35 razmaka umesto naziva primaoca, 20 razmaka umesto mesta i 3 razmaka umesto šifre plaćanja. | Fajl je sintaksno ispravan (fiksne širine se poštuju), pa se problem vidi tek kada Halcom/Intesa odbije paket ili proknjiži naloge bez naziva. |
| `placanja/payment-export.service.ts:107-109,137-160` | **V** | Zaglavlje sabira `Σ o.amount` **bez obzira na `PaymentOrder.currency`** i tvrdo upisuje `"YUM"`. Nalog na 1.000 EUR i nalog na 50.000 RSD odu u isti fajl kao ukupno 51.000 dinara. Uz nalaz 1.16 (EUR nalog sa RSD iznosom) — devizni nalozi ovim putem ne smeju ni da postoje, a ništa ih ne sprečava. | Kontrolni zbir u zaglavlju se **slaže** sa zbirom slogova, pa fajl prolazi bančinu formalnu kontrolu. |
| `placanja/payment-export.service.ts:169-177` | **S** | Nalozi se prebacuju u `PAID` + `exportedAt` **odmah po generisanju TXT-a**, pre nego što se zna da je korisnik fajl uopšte preuzeo. Pad mreže na `POST /placanja/export` → nalozi su PAID, fajl je izgubljen, ponovni izvoz je `409`. Fajl se nigde ne čuva. | Statusi u listi izgledaju uredno („plaćeno"); banka nikad nije videla naloge. |
| `placanja/payment-export.service.ts:222-228` | **S** | `formatAmount13` radi `.abs()` (negativan iznos ode u banku kao pozitivan) i `.slice(-11)` (iznos ≥ 100 mlrd tiho gubi vodeće cifre). | Fiksna širina je zadovoljena, pa nema greške pri generisanju. |
| `placanja/mod97.util.ts:34-36,91-106` | **S** | Osnova PNB-a je `digitsOnly(documentNumber)`. Dve različite fakture istog dobavljača `„26/12"` i `„2612"` daju **isti** poziv na broj → `@@unique(referenceNumberCredit, supplierId)` → druga, legitimna isplata odbijena sa „dvostruko plaćanje odbijeno". | Poruka je uverljiva; korisnik ne zna da su brojevi „isti" tek posle skidanja kose crte. |
| `schema.prisma:4079` + `payment-preparation.service.ts:404-407` | **S** | `PaymentOrder.orderNumber` **nema unique**, a bez `seriesNumber` se generiše `"AUTO-1"`, `"AUTO-2"`… Svaki sledeći batch počinje ponovo od `AUTO-1`. U banci i u pregledu naloga postoji pet naloga sa brojem `AUTO-1`. | Broj naloga nigde nije ključ pretrage, pa se duplikat vidi tek u izvozu ili reklamaciji. |
| `saldakonti/compensation.service.ts:62,213-223,383-386` | **V** | `ReconciliationService` je injektovan i **nikad korišćen**; `closeGroup(kind: "COMPENSATION")` postoji u `reconciliation.service.ts:355` i nikad se ne poziva. Kompenzacija upisuje `reconciledAt` **bez** `reconciliationGroupId` → `POST /saldakonti/reconcile/unreconcile` (koji briše po `reconciliationGroupId`) **ne može da razveže pogrešnu kompenzaciju**. Jedini izlaz je ručni UPDATE u bazi. Uz to, `status: "CONFIRMED"` iz šeme se nikad ne koristi, a DRAFT kompenzacija se **ne može naknadno proknjižiti** (`post` postoji samo na `create`). | Kompenzacija u listi ima status POSTED i journalEntryId — izgleda kao potpuno zaokružen dokument. |
| domen (grep) | **V** | **Cesija ne postoji nigde** u kodu (ni model, ni ruta, ni PDF). Ustupanje potraživanja trećem licu se u praksi radi kroz `manualReconcile` bez traga o pravnom osnovu. | Kompenzacija postoji i liči na isto, pa se rupa vidi tek kad zatreba. |
| `saldakonti/reconciliation.service.ts:164-207` | **S** | **Otpis** je samo flag: „Razlika (sitni saldo) se NE knjiži kao otpis ovde". `MaxSaldo` postavi `reconciledAt` i tvrdo je ograničen na 1.000 din (`MAX_SMALL_BALANCE_THRESHOLD`). Otpis nenaplativog potraživanja od 400.000 (zastarelo, stečaj) mora ručno: nalog GK + ručni reconcile, bez ijedne evidencije razloga osim `note`. | Postojanje „MaxSaldo" dugmeta stvara utisak da je otpis pokriven. |
| `saldakonti.controller.ts:140-166` | **S** | `POST /saldakonti/ios-pdf/send-mail` **ništa ne upisuje** — nema traga ko je, kome i kada poslao IOS, ni polja za „primljeno / osporeno / iznos po komitentu". Zakonsko usaglašavanje 31.12. se vodi u Excelu pored sistema. Za opomene evidencija (`DunningNotice`) postoji — za IOS ne. | Ruta vrati `{ sent: true }`, mejl stigne; da nema evidencije, primeti se tek u januaru. |
| `saldakonti/dunning.service.ts:269-280` | **S** | Anti-duplo je vezano **samo za kalendarski dan**. Kupac koji kasni 90 dana svakog dana kada se pokrene `send-batch` dobija novu „OPOMENU PRED UTUŽENJE" — nema minimalnog razmaka između nivoa, nema eskalacije stanja, nema `storno` opomene. Nivo se svaki put iznova izvodi iz aging-a. | Svaki pojedinačni mejl je ispravan i evidentiran; niz od 30 istih mejlova vidi samo kupac. |
| ceo domen | **S** | Nijedan servis osim `fx-revaluation` i `open-items` ne filtrira po `companyId`; `bank-statement` i `compensation` hardkoduju firmu 0. Kompenzacija, kamata, opomene i priprema plaćanja bi u multi-firma režimu netovali stavke različitih pravnih lica. | Instalacija je jednofirmska — dok ne bude. |

---

## 4. Greška koja stiže kao 500 umesto jasne poruke

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `placanja/placanja.controller.ts:56-58` → `payment-preparation.service.ts:326-341` | **V** | `GET /placanja/due?cutoff=31.12.2026` → `new Date(...)` = Invalid Date. `dueMs > cutMs` je `false` za sve (NaN poređenje) → **sve** obaveze se proglase dospelim, `daysOverdue` je NaN, sort je nedefinisan, pa `cutoff.toISOString()` baci `RangeError: Invalid time value` → **500**. | Ako bi ruta nekim putem prošla (npr. bez `meta`), korisnik bi dobio predlog za plaćanje svih nedospelih obaveza — što je gore od 500. |
| `kamata/kamata.service.ts:140-146` | **S** | `POST /kamata/compute` sa `calcDate: "31.12.2026"` → Invalid Date → Prisma dobije Invalid Date u `where.validFrom.lte` → `PrismaClientValidationError` → **500 „Internal server error"**. Ostala polja (`partnerId`, `kind`, `ratePct`) su validirana; datum nije. | Poruka ne pominje datum, pa korisnik traži grešku u komitentu ili stopi. |
| `saldakonti/reconciliation.service.ts:192-206` | **V** | `POST /saldakonti/reconcile/small-balances` sa pragom 1.000 nad prod bazom: `listOpenItems()` bez filtera, pa **2 upita po grupi u jednoj interaktivnoj transakciji**. Na 5.000 sitnih salda = 10.000 upita → Prisma default timeout 5 s → `P2028` → **500**, batch se rollback-uje. Korisnik ponovi tri puta pa odustane. | Poruka je generička; na dev bazi (prazna) radi trenutno. |
| `saldakonti/compensation.service.ts:301,310,328-335` | **V** | `postCompensation` **tiho preskače** liniju čiji `ledgerEntryId` ne postoji (`continue`). Time se pokvari bilateralni bilans koji je `create` proverio nad DTO-om → `postManualEntry` baci `LedgerNotBalancedException`, koja **nije HttpException** → **goli 500**, cela kompenzacija rollback. | Korisnik je video „balansira" na ekranu pripreme; 500 nema veze sa uzrokom (nepostojeća stavka). |
| `izvodi/izvodi.controller.ts:69-74` | **S** | `GET /izvodi?skip=abc` → `Number("abc")` = NaN → Prisma `skip: NaN` → **500**. Isto na `placanja.controller.ts:78-80` (`skip`/`take`/`supplierId`). | Paginacija se u FE-u uvek šalje ispravno; ruši se samo ručni/skriptovani poziv. |
| `izvodi/izvodi.controller.ts:152-160` → `bank-statement.service.ts:492` | **S** | `POST /izvodi/:id/lines/:lineId/link` bez tela → `body?.ledgerEntryId` = `undefined` → `findUnique({ where: { id: undefined } })` → Prisma greška → **500** umesto 400 „ledgerEntryId je obavezan". | Rutu zove samo FE dugme koje uvek šalje ID. |
| `saldakonti/saldakonti.controller.ts:256-264` | **N** | `GET /saldakonti/compensation/proposal` bez `partnerId` vraća **HTTP 200** sa `{ data: null, meta: { error: "partnerId je obavezan." } }` — jedina ruta u kontroleru koja grešku vraća kao uspeh (ostale bacaju `BadRequestException`). | FE koji čita `data` dobije `null` i prikaže prazan predlog. |
| `placanja/payment-preparation.service.ts:688-696` | **N** | Nevalidan/budući `beforeDate` baca `ConflictException` (**409**) umesto 400 — kontroler je već validirao datum, pa je 409 ovde mrtav kod sa pogrešnim kodom. | Nikad se ne okine kroz FE. |

---

## 5. Performansa pri rastu

| fajl:linija | ozb. | scenario | zašto se ne primeti |
|---|---|---|---|
| `izvodi/bank-statement.service.ts:176-199` | **V** | Do **5 upita po redu izvoda** (2× `customer.findMany` + 2× `ledgerEntry` + 1 `update`), sekvencijalno, van transakcije. Izvod od 300 stavki = ~1.500 round-tripova; „Upari" traje ~20 s uz zaključavanje ekrana. Uz rast `ledger_entries` (BigBit uvoz do 2027.) `matchOpenItem` fallback po iznosu skenira sve otvorene stavke komitenta. | Na test-izvodu od 10 stavki je trenutno. |
| `placanja/payment-preparation.service.ts:137-154` | **V** | `GET /placanja/due` učitava **sve** otvorene stavke svih payable konta u memoriju (`findMany` bez `take`, bez `companyId`, bez datumskog opsega) i grupiše u JS-u. Posle uvoza BigBit istorije to su stotine hiljada redova na sinhronoj GET ruti. | Tabele su danas prazne (`ERP 4.0 test pilot` — tabele prazne), pa je ruta trenutna. |
| `saldakonti/open-items.service.ts:193-232` + `saldakonti.controller.ts:86-96` | **V** | `GET /saldakonti/open-items` **nema LIMIT ni paginaciju** — vraća svaku otvorenu stavku firme u jednom JSON-u, sa `array_agg(le.id)` po grupi. Isto važi za `GET /saldakonti/aging`. | `meta.count` postoji ali nema `skip/take` — izgleda kao namerna „puna lista". |
| `saldakonti/collection-dashboard.service.ts:75-78` | **S** | Svako otvaranje table naplate = **dva puna skena** `ledger_entries × journal_entries × saldakonto_accounts` (`listOpenItems` + `agingByPartner`), oba bez indeksa na `posting_date`. | Tabla je „samo pregled", pa se sporost pripiše mreži. |
| `saldakonti/dunning.service.ts:99,375-379` | **S** | `candidates()` skenira sve otvorene stavke, pa `loadHistory` radi `dunningNotice.findMany` **bez `take`** za sve kandidate — posle godinu dana dnevnog batch-a to je cela istorija opomena u memoriji, samo da bi se našla poslednja po partneru. | Prva godina rada je jeftina. |
| `saldakonti/compensation.service.ts:302-309,346-355` | **S** | Po liniji kompenzacije: 1 `findUnique` + 1 `findMany` **unutar** transakcije koja je otvorena od `create`. Multilateralna kompenzacija sa 200 linija drži transakciju i redove zaključane dok traje 400 upita. | Bilateralne kompenzacije imaju 2–6 linija. |
| `saldakonti/fx-revaluation.service.ts:372-378` | **S** | `run()` iznova poziva `preview()` (dva puna skena otvorenih stavki + upit kursa) **van** transakcije, pa tek onda otvara transakciju. Na 31.12. sa punom bazom to je najduža ruta u domenu, a i prozor za trku (`assertMatchesPreview` je opcion — radi samo ako FE pošalje očekivane vrednosti). | Radi se jednom godišnje po valuti. |
| `saldakonti/partner-card.service.ts:167-194` | **S** | Kartica komitenta bez `from`/`to` vuče **celu istoriju** partnera u jedan odgovor i u jedan PDF (running saldo se računa u JS-u). Za velikog kupca posle migracije to je 10.000+ redova. | Podrazumevani period u FE-u verovatno postoji; ruta ga ne zahteva. |

---

## TOP 10 ZA OVAJ DOMEN

Redosled = po očekivanoj šteti × verovatnoći, ne po težini popravke.

| # | nalaz | fajl:linija | trud | zašto baš ovim redom |
|---|---|---|---|---|
| 1 | **Prazan `saldakonto_accounts` gasi ceo domen tiho** — IOS/aging/opomene/kamata/priprema plaćanja prazni, kreditni limit isključen | seed migracija `20260726100000_…` je **untracked**; INNER JOIN u `open-items.service.ts:212` | **S** | Jedna migracija otključava (ili trajno kvari) sve ostalo. Dok ovo stoji, svi ostali nalazi su nemerljivi. Uz komit migracije dodati **fail-fast**: ako je registar prazan, čitaoci moraju vraćati 422 „saldakonto registar nije podešen", nikad `[]`. |
| 2 | **Fallback po iznosu zatvara pogrešnu (već plaćenu) fakturu** + uplata nikad ne postavlja `reconciled_at` | `izvodi/bank-statement.service.ts:329-336`, `:280-284`, `:764` | **M** | Direktno pravi lažni dug i lažnu preplatu, iz čega odmah slede kamata i opomena pogrešnom kupcu. Lek: `baseWhere` proširiti na saldakonto konta + smer + `orderBy`, a fallback po iznosu dozvoliti **samo** kad je pogodak jedinstven; posle knjiženja pozvati `ReconciliationService`. |
| 3 | **Opomena i kamata gađaju dobavljače** (`side` bez `partner_scope`) | `dunning.service.ts:104,400`, `dunning-pdf.service.ts:115`, `kamata.service.ts:161` | **S** | Jedan uslov po čitaocu; posledica je „opomena pred utuženje" dobavljaču — reputaciona i pravna. `assertCreditLimit` već ima tačan obrazac za kopiranje. |
| 4 | **Kompenzacija: nema provere vlasništva stavke, nema statusa naloga, nema undo-a** | `dto/saldakonti.dto.ts:88-120`, `compensation.service.ts:300-317,346-355,62` | **M** | Jedini tok u domenu koji sme da zatvori tuđi dug bez ijedne provere, i jedini bez puta nazad (`unreconcile` ga ne vidi). Tri popravke u istom fajlu: validacija linija, POSTED/LOCKED u upit članova, `closeGroup(kind:"COMPENSATION")`. |
| 5 | **Stavke izvoda tiho nestaju, a kontrola salda je isključena** | `bank-statement-parser.service.ts:82-90,142-150`, `bank-statement.service.ts:1056-1071` | **S** | Novac koji je stigao na račun ne postoji u sistemu, i nijedan mehanizam to ne prijavljuje. Lek: `parse` vraća broj preskočenih redova (i uvoz pada ako ih ima), nepoznat `DugPotInd` = greška a ne CREDIT, opening/closing obavezni pri uvozu iz TXT-a. |
| 6 | **Devizna uplata bez deviznog para + tvrdo 2040/4350 po smeru** | `bank-statement.service.ts:669-712,642-643` | **M** | Kvari i saldakonta i bilansni presek 31.12.; danas se maskira kroz `RATE_MISMATCH` flag pa izgleda kao „podatak za doradu". Konto uzeti iz `saldakonto_accounts` po `partner_scope`, a FX linije popuniti iz `foreignAmount/exchangeRate` koje stavka već nosi. |
| 7 | **Priprema plaćanja: nemoguće delimično plaćanje, nemoguće otkazati nalog, valuta iz pogrešne kolone** | `payment-preparation.service.ts:413-423,189`, nema rute za storno | **M** | Blokira svakodnevni rad (rate dobavljačima) i može poslati EUR nalog sa RSD iznosom. Dedup vezati za **aktivne** naloge (`status != CANCELLED`) i uporediti Σ naloga sa otvorenim saldom, umesto „jedan nalog po stavci zauvek". |
| 8 | **Nevalidan datum tiho postaje „danas"** u celom saldakonti kontroleru | `saldakonti.controller.ts:506-516` | **S** | Šest ruta, uključujući IOS obrazac koji se potpisuje. Dve funkcije: baciti 400 umesto vraćanja `undefined`. Najjeftiniji nalaz sa najvišim odnosom šteta/trud. |
| 9 | **Izvoz u banku je nekompletan i nepovratan** — prazan naziv primaoca, mešane valute u „YUM" zaglavlju, PAID pre isporuke fajla | `payment-export.service.ts:238-246,107-109,169-177` | **M** | Fajl koji ide u banku ne sme da bude „skoro dobar". Uz naziv primaoca dodati guard na jedinstvenu valutu i čuvati generisani fajl (ili markirati PAID tek na potvrdu). |
| 10 | **Tabla naplate protivreči sama sebi** (`totalReceivable` vs `aging.total`) i **kamata se sistematski potcenjuje** netiranjem | `collection-dashboard.service.ts:75-97` + `open-items.service.ts:336`; `kamata.service.ts:205-238` | **L** | Oba traže odluku knjigovođe (koja je definicija „potraživanja" na tabli; da li kamata ide po periodima), pa ne mogu ući u brzi talas — ali su jedini nalazi koji trajno kvare **izveštajnu** istinu i moraju biti na dnevnom redu pre prvog paralelnog PDV obračuna. |

**Predložen redosled izvođenja:** 1 → 8 → 3 → 5 → 2 → 4 → 7 → 6 → 9 → 10.
(1 i 8 su „S" i otključavaju merenje svega ostalog; 10 čeka odluku, ne kod.)

---

### Napomena o metodu

Sve tvrdnje su iz čitanja koda na `C:\Users\nenad.jarakovic\wt\robno-quality\backend\src` (read-only).
Nijedan fajl u toj bazi nije menjan. Scenariji sa konkretnim brojevima (657/25, 1.000 EUR /
117.000 RSD, avans 1520/500.000) preuzeti su iz komentara u samom kodu gde postoje, da bi bili
uporedivi sa ranijim merenjima tima; ostali su izvedeni iz predikata u upitima.
