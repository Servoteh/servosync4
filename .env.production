# Produkcijski build (static export → Cloudflare Pages).
# NEXT_PUBLIC_* se INLINE-uje u klijentski JS pri build-u — nije tajna, ali mora
# da pokazuje na backend kroz Cloudflare Tunnel.
NEXT_PUBLIC_API_URL=https://api.servosync2.servoteh.com/api
