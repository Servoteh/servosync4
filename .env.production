# Produkcijski build (static export → Cloudflare Workers static assets).
# NAPOMENA: od runtime-resolve izmene, API base se bira U BROWSERU (src/api/client.ts):
#   preko Cloudflare-a → Tunnel; na LAN-u → http://<isti-host>:3000/api.
# Ova promenljiva je sad SAMO fallback za slučaj bez window-a (prerender/testovi) —
# menjanje ove vrednosti NE utiče na ponašanje u browseru. Ne treba rebuild za LAN.
NEXT_PUBLIC_API_URL=https://api.servosync2.servoteh.com/api
